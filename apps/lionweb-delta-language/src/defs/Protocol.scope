scoper for language Protocol 

isNamespace { Types, MessageGroup, ObjectType }

// MessageGroup {
//     imports {
//         self.owner().typeDefinitions;
//     }
// }

// NB: The error message on `types` is incorrect, it is correct and everything works as it should.
ObjectType {
    alternatives {
        self.owner().if(MessageGroup).owner().types;
        self.owner().if(MessageGroup).owner();
        self.owner().if(Types).owner().types;
        self.owner().if(Types).owner();
    }
}

MessageGroup {
    imports {
        self.owner().types;
    }
}

Types {
    imports {
        self.owner().types;
    }
}

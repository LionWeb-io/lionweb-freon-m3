// Generated by the Freon Language Generator.
// TEMPLATE EnvironmentTemplate.generateEnvironment(...)

import {
    FreEditor,
    type FreEnvironment,
    type FreReader,
    FreCompositeTyper,
    type FreValidator,
    type FreStdlib,
    type FreWriter,
    type FreInterpreter,
    FreCompositeScoper,
    FreLanguageEnvironment,
    FreProjectionHandler,
} from "@freon4dsl/core";
import { LionCore_M3, Language, initializeLanguage } from "../../language/gen/index.js";

import { LionCore_M3Actions, initializeEditorDef, initializeProjections } from "../../editor/gen/index.js";

import { initializeScoperDef } from "../../scoper/gen/index.js";
import { initializeTypers } from "../../typer/gen/index.js";
import { LionCore_M3Validator } from "../../validator/gen/index.js";
import { LionCore_M3Stdlib } from "../../stdlib/gen/LionCore_M3Stdlib.js";
import { LionCore_M3ModelUnitWriter } from "../../writer/gen/LionCore_M3ModelUnitWriter.js";
import { LionCore_M3ModelUnitReader } from "../../reader/gen/LionCore_M3ModelUnitReader.js";
import { MainLionCore_M3Interpreter } from "../../interpreter/MainLionCore_M3Interpreter.js";

/**
 * Class LionCore_M3Environment provides the link between all parts of the language environment.
 * It holds the currently used editor, scoper, typer, etc, thus providing an entry point for,
 * for instance, the editor to find the right scoper, or for the validator to find the typer
 * to use.
 * This class uses the singleton pattern to ensure that only one instance of the class is present.
 */
export class LionCore_M3Environment implements FreEnvironment {
    private static environment: FreEnvironment; // the only instance of this class

    /**
     * This method implements the singleton pattern
     */
    public static getInstance(): FreEnvironment {
        if (this.environment === undefined || this.environment === null) {
            this.environment = new LionCore_M3Environment();
            FreLanguageEnvironment.setInstance(this.environment);
        }
        return this.environment;
    }

    /**
     * A private constructor, as demanded by the singleton pattern.
     */
    private constructor() {
        const actions = new LionCore_M3Actions();
        const myComposite = new FreProjectionHandler();
        this.editor = new FreEditor(myComposite, this, actions);
        initializeLanguage();
        initializeProjections(myComposite);
        initializeEditorDef();
        initializeScoperDef(this.scoper);
        initializeTypers(this.typer);
        this.projectionHandler = myComposite;
    }

    /**
     * Returns a new model with name 'modelName'.
     *
     * @param modelName
     */
    newModel(modelName: string): LionCore_M3 {
        const model = new LionCore_M3();
        model.name = modelName;
        return model;
    }

    // the parts of the language environment
    editor: FreEditor;
    scoper: FreCompositeScoper = new FreCompositeScoper();
    typer: FreCompositeTyper = new FreCompositeTyper("main");
    validator: FreValidator = new LionCore_M3Validator();
    writer: FreWriter = new LionCore_M3ModelUnitWriter();
    reader: FreReader = new LionCore_M3ModelUnitReader();
    interpreter: FreInterpreter = new MainLionCore_M3Interpreter();
    projectionHandler: FreProjectionHandler;
    languageName: string = "LionCore_M3";
    fileExtensions: Map<string, string> = new Map([["Language", "lan"]]);
}

// Generated by the Freon Language Generator.
// TEMPLATE BoxProviderTemplate.generateBoxProvider(...)

import { Box, BoxUtil, FreBoxProvider, FreProjectionHandler, BoxFactory, FreListInfo } from "@freon4dsl/core";
import { Language } from "../../language/gen/index.js";

import { LanguageEnvironment } from "../../index.js";

/**
 * This class implements the box provider for a single node of type Language.
 * The box provider is able to create the (tree of) boxes for the node, based
 * on the projections that are currently selected by the user.
 * The top of the tree of boxes is always a box of type ElementBox, which is
 * a box that will never be rendered itself, only its content will. Thus, we
 * have a stable entry in the complete box tree for every FreNode node.
 */
export class LanguageBoxProvider extends FreBoxProvider {
    constructor(mainHandler: FreProjectionHandler) {
        super(mainHandler);
        this.knownBoxProjections = ["textual", "default"];
        this.knownTableProjections = ["default"];
        this.conceptName = "Language";
    }

    protected getContent(projectionName: string): Box {
        // console.log("GET CONTENT " + this._node?.freId() + ' ' +  this._node?.freLanguageConcept() + ' ' + projectionName);
        // see if we need to use a custom projection
        if (!this.knownBoxProjections.includes(projectionName) && !this.knownTableProjections.includes(projectionName)) {
            const BOX: Box = this.mainHandler.executeCustomProjection(this._node, projectionName);
            if (!!BOX) {
                // found one, so return it
                return BOX;
            }
        } else {
            // select the box to return based on the projectionName
            if (projectionName === "textual") {
                return this.getTextual();
            } else if (projectionName === "default") {
                return this.getDefault();
            }
        }
        // in all other cases, return the default
        return this.getDefault();
    }

    private getTextual(): Box {
        return BoxFactory.verticalLayout(
            this._node as Language,
            "Language-overall",
            "",
            [
                BoxFactory.horizontalLayout(
                    this._node as Language,
                    "Language-hlist-line-0",
                    "",
                    [
                        BoxUtil.labelBox(this._node as Language, "language", "top-1-line-0-item-0"),
                        BoxUtil.textBox(this._node as Language, "name"),
                        BoxUtil.labelBox(this._node as Language, "key", "top-1-line-0-item-2"),
                        BoxUtil.textBox(this._node as Language, "key"),
                    ],
                    { selectable: false },
                ),
                BoxFactory.horizontalLayout(
                    this._node as Language,
                    "Language-hlist-line-1",
                    "",
                    [
                        BoxUtil.labelBox(this._node as Language, "version", "top-1-line-1-item-0"),
                        BoxUtil.textBox(this._node as Language, "version"),
                    ],
                    { selectable: false },
                ),
                BoxFactory.horizontalLayout(
                    this._node as Language,
                    "Language-hlist-line-2",
                    "",
                    [
                        BoxUtil.labelBox(this._node as Language, "depends on languages:", "top-1-line-2-item-0"),
                        BoxUtil.verticalReferenceListBox(
                            this._node as Language,
                            "dependsOn",
                            LanguageEnvironment.getInstance().scoper,
                            FreListInfo.NullListInfo,
                        ),
                    ],
                    { selectable: false },
                ),
                BoxUtil.emptyLineBox(this._node as Language, "Language-empty-line-3"),
                BoxUtil.verticalPartListBox(
                    this._node as Language,
                    (this._node as Language).entities,
                    "entities",
                    FreListInfo.NullListInfo,
                    this.mainHandler,
                ),
            ],
            { cssClass: "Language" },
        );
    }

    private getDefault(): Box {
        return BoxFactory.verticalLayout(
            this._node as Language,
            "Language-overall",
            "",
            [
                BoxFactory.horizontalLayout(
                    this._node as Language,
                    "Language-hlist-line-0",
                    "",
                    [
                        BoxUtil.labelBox(this._node as Language, "language", "top-1-line-0-item-0"),
                        BoxUtil.textBox(this._node as Language, "name"),
                        BoxUtil.labelBox(this._node as Language, "key", "top-1-line-0-item-2"),
                        BoxUtil.textBox(this._node as Language, "key"),
                    ],
                    { selectable: false },
                ),
                BoxFactory.horizontalLayout(
                    this._node as Language,
                    "Language-hlist-line-1",
                    "",
                    [
                        BoxUtil.labelBox(this._node as Language, "version", "top-1-line-1-item-0"),
                        BoxUtil.textBox(this._node as Language, "version"),
                    ],
                    { selectable: false },
                ),
                BoxFactory.horizontalLayout(
                    this._node as Language,
                    "Language-hlist-line-2",
                    "",
                    [
                        BoxUtil.labelBox(this._node as Language, "depends on languages:", "top-1-line-2-item-0"),
                        BoxUtil.verticalReferenceListBox(
                            this._node as Language,
                            "dependsOn",
                            LanguageEnvironment.getInstance().scoper,
                            FreListInfo.NullListInfo,
                        ),
                    ],
                    { selectable: false },
                ),
                BoxUtil.emptyLineBox(this._node as Language, "Language-empty-line-3"),
                BoxUtil.partListReplacerBox(
                    this._node as Language,
                    (this._node as Language).entities,
                    "entities",
                    "ExternalAccordion",
                    this.mainHandler,
                ),
            ],
            { cssClass: "Language" },
        );
    }
}

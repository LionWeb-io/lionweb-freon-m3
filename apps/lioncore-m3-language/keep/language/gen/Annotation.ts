// Generated by the Freon Language Generator.
// TEMPLATE ConceptTemplate.generateConceptPrivate(...)

import {
    observablepart,
    observablepartlist,
    type FreNode,
    FreParseLocation,
    notNullOrUndefined,
    FreNodeReference,
    matchReferenceList,
} from "@freon4dsl/core";
import { Classifier, Interface } from "./internal.js";

// TEMPLATE: ConceptTemplate.generateConceptPrivate

/**
 * Class Annotation is the implementation of the concept with the same name in the language definition file.
 * It uses mobx decorators to enable parts of the language environment, e.g. the editor, to react
 * to the changes in the state of its properties.
 */
export class Annotation extends Classifier implements FreNode {
    /**
     * A convenience method that creates an instance of this class
     * based on the properties defined in 'data'.
     * @param data partial object
     */
    static create(data: Partial<Annotation>): Annotation {
        const result = new Annotation(data.$id);
        if (notNullOrUndefined(data.name)) {
            result.name = data.name;
        }
        if (notNullOrUndefined(data.key)) {
            result.key = data.key;
        }
        if (notNullOrUndefined(data.features)) {
            data.features.forEach((x) => result.features.push(x));
        }
        if (notNullOrUndefined(data.annotates)) {
            result.annotates = data.annotates;
        }
        if (notNullOrUndefined(data.extends)) {
            result.extends = data.extends;
        }
        if (notNullOrUndefined(data.implements)) {
            data.implements.forEach((x) => result.implements.push(x));
        }
        if (notNullOrUndefined(data.parseLocation)) {
            result.parseLocation = data.parseLocation;
        }
        return result;
    }

    readonly $typename: string = "Annotation"; // holds the metatype in the form of a string

    parseLocation: FreParseLocation | undefined; // if relevant, the location of this element within the source from which it is parsed

    annotates: FreNodeReference<Classifier> | undefined; // implementation of reference 'annotates'
    extends: FreNodeReference<Annotation> | undefined; // implementation of reference 'extends'
    implements!: FreNodeReference<Interface>[]; // implementation of reference 'implements'

    constructor(id?: string) {
        super(id);

        // Both 'observablepart' and 'observablepartlist' change the get and set of the attribute
        // such that the parent-part relationship is consistently maintained,
        // and make sure the part is observable. In lists no 'null' or 'undefined' values are allowed.
        observablepart(this, "annotates");

        observablepart(this, "extends");

        observablepartlist(this, "implements");
    }

    /**
     * Returns the metatype of this instance in the form of a string.
     */
    freLanguageConcept(): string {
        return this.$typename;
    }

    /**
     * Returns true if this instance is a model concept.
     */
    freIsModel(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a model unit.
     */
    freIsUnit(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is an expression concept.
     */
    freIsExpression(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a binary expression concept.
     */
    freIsBinaryExpression(): boolean {
        return false;
    }
    /**
     * A convenience method that copies this instance into a new object.
     */
    copy(): Annotation {
        const result = new Annotation();
        if (notNullOrUndefined(this.name)) {
            result.name = this.name;
        }
        if (notNullOrUndefined(this.key)) {
            result.key = this.key;
        }
        if (notNullOrUndefined(this.features)) {
            this.features.forEach((x) => result.features.push(x.copy()));
        }
        if (notNullOrUndefined(this.annotates)) {
            result.annotates = this.annotates.copy();
        }
        if (notNullOrUndefined(this.extends)) {
            result.extends = this.extends.copy();
        }
        if (notNullOrUndefined(this.implements)) {
            this.implements.forEach((x) => result.implements.push(x.copy()));
        }
        return result;
    }
    /**
     * Matches a partial instance of this class to this object
     * based on the properties defined in the partial.
     * @param toBeMatched
     */
    public match(toBeMatched: Partial<Annotation>): boolean {
        let result: boolean = super.match(toBeMatched);
        if (result && notNullOrUndefined(toBeMatched.annotates)) {
            if (this.annotates) {
                result = result && this.annotates.match(toBeMatched.annotates);
            }
        }
        if (result && notNullOrUndefined(toBeMatched.extends)) {
            if (this.extends) {
                result = result && this.extends.match(toBeMatched.extends);
            }
        }
        if (result && notNullOrUndefined(toBeMatched.implements)) {
            result = result && matchReferenceList(this.implements, toBeMatched.implements);
        }
        return result;
    }

    /**
     * Convenience method for reference 'annotates'.
     * Instead of returning a 'FreNodeReference<Classifier>' object,
     * it returns the referred 'Classifier' object, if it can be found.
     */
    get $annotates(): Classifier | undefined {
        if (!!this.annotates) {
            return this.annotates.referred;
        }
        return undefined;
    }
    /**
     * Convenience method for reference 'extends'.
     * Instead of returning a 'FreNodeReference<Annotation>' object,
     * it returns the referred 'Annotation' object, if it can be found.
     */
    get $extends(): Annotation | undefined {
        if (!!this.extends) {
            return this.extends.referred;
        }
        return undefined;
    }
    /**
     * Convenience method for reference 'implements'.
     * Instead of returning a list of 'FreNodeReference<Interface>' objects, it
     * returns a list of referred 'Interface' objects, if the references can be resolved.
     *
     * Note that when some references cannot be resolved, the length of this list is
     * different to the length of 'implements'.
     */
    get $implements(): readonly Interface[] {
        const result: Interface[] = [];
        for (const $part of this.implements) {
            if (!!$part.referred) {
                result.push($part.referred);
            }
        }
        return result;
    }
}

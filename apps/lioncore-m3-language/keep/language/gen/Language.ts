// Generated by the Freon Language Generator.
// TEMPLATE: UnitTemplate.generateUnit(...)

import {
    MobxModelElementImpl,
    observableprim,
    observablepartlist,
    type FreModelUnit,
    FreParseLocation,
    notNullOrUndefined,
    FreNodeReference,
    FreUtils,
    matchElementList,
    matchReferenceList,
} from "@freon4dsl/core";
import { LanguageEntity, type IKeyed } from "./internal.js";

import { makeObservable, action } from "mobx";

/**
 * Class Language is the implementation of the model unit with the same name in the language definition file.
 * It uses mobx decorators to enable parts of the language environment, e.g. the editor, to react
 * to any changes in the state of its properties.
 */
export class Language extends MobxModelElementImpl implements FreModelUnit, IKeyed {
    /**
     * A convenience method that creates an instance of this class
     * based on the properties defined in 'data'.
     * @param data partial object
     */
    static create(data: Partial<Language>): Language {
        const result = new Language(data.$id);
        if (notNullOrUndefined(data.version)) {
            result.version = data.version;
        }
        if (notNullOrUndefined(data.key)) {
            result.key = data.key;
        }
        if (notNullOrUndefined(data.name)) {
            result.name = data.name;
        }
        if (notNullOrUndefined(data.entities)) {
            data.entities.forEach((x) => result.entities.push(x));
        }
        if (notNullOrUndefined(data.dependsOn)) {
            data.dependsOn.forEach((x) => result.dependsOn.push(x));
        }
        if (notNullOrUndefined(data.parseLocation)) {
            result.parseLocation = data.parseLocation;
        }
        return result;
    }

    fileExtension: string = "";
    readonly $typename: string = "Language"; // holds the metatype in the form of a string
    $id: string = ""; // a unique identifier
    parseLocation: FreParseLocation | undefined; // if relevant, the location of this element within the source from which it is parsed
    version!: string; // implementation of version
    key!: string; // implementation of key
    name!: string; // implementation of name
    entities!: LanguageEntity[]; // implementation of part 'entities'
    dependsOn!: FreNodeReference<Language>[]; // implementation of reference 'dependsOn'

    constructor(id?: string) {
        super();
        if (!!id) {
            this.$id = id;
        } else {
            this.$id = FreUtils.ID(); // uuid.v4();
        }
        // Both 'observableprim' and 'observableprimlist' change the get and set of the attribute
        // such that the part is observable. In lists no 'null' or 'undefined' values are allowed.
        observableprim(this, "version");
        this.version = "";
        observableprim(this, "key");
        this.key = "";
        observableprim(this, "name");
        this.name = "";

        // Both 'observablepart' and 'observablepartlist' change the get and set of the attribute
        // such that the parent-part relationship is consistently maintained,
        // and make sure the part is observable. In lists no 'null' or 'undefined' values are allowed.
        observablepartlist(this, "entities");
        observablepartlist(this, "dependsOn");

        // Make copy method a mobx action
        makeObservable(this, {
            copy: action,
        });
    }

    /**
     * Returns the metatype of this instance in the form of a string.
     */
    freLanguageConcept(): string {
        return this.$typename;
    }

    /**
     * Returns the unique identifier of this instance.
     */
    freId(): string {
        return this.$id;
    }

    /**
     * Returns true if this instance is a model concept.
     */
    freIsModel(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a model unit.
     */
    freIsUnit(): boolean {
        return true;
    }

    /**
     * Returns true if this instance is an expression concept.
     */
    freIsExpression(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a binary expression concept.
     */
    freIsBinaryExpression(): boolean {
        return false;
    }
    /**
     * A convenience method that copies this instance into a new object.
     */
    copy(): Language {
        const result = new Language();
        if (notNullOrUndefined(this.version)) {
            result.version = this.version;
        }
        if (notNullOrUndefined(this.key)) {
            result.key = this.key;
        }
        if (notNullOrUndefined(this.name)) {
            result.name = this.name;
        }
        if (notNullOrUndefined(this.entities)) {
            this.entities.forEach((x) => result.entities.push(x.copy()));
        }
        if (notNullOrUndefined(this.dependsOn)) {
            this.dependsOn.forEach((x) => result.dependsOn.push(x.copy()));
        }
        return result;
    }
    /**
     * Matches a partial instance of this class to this object
     * based on the properties defined in the partial.
     * @param toBeMatched
     */
    public match(toBeMatched: Partial<Language>): boolean {
        let result: boolean = true;
        if (result && toBeMatched.version !== null && toBeMatched.version !== undefined && toBeMatched.version.length > 0) {
            result = result && this.version === toBeMatched.version;
        }
        if (result && toBeMatched.key !== null && toBeMatched.key !== undefined && toBeMatched.key.length > 0) {
            result = result && this.key === toBeMatched.key;
        }
        if (result && toBeMatched.name !== null && toBeMatched.name !== undefined && toBeMatched.name.length > 0) {
            result = result && this.name === toBeMatched.name;
        }
        if (result && notNullOrUndefined(toBeMatched.entities)) {
            result = result && matchElementList(this.entities, toBeMatched.entities);
        }
        if (result && notNullOrUndefined(toBeMatched.dependsOn)) {
            result = result && matchReferenceList(this.dependsOn, toBeMatched.dependsOn);
        }
        return result;
    }
}

// Generated by the Freon Language Generator.
// TEMPLATE WriterTemplate.generateUnparser(...)

import { type FreNamedNode, FreNodeReference, type FreWriter, type FreNode, isNullOrUndefined, notNullOrUndefined } from "@freon4dsl/core";
import {
    Node,
    Annotation,
    Concept,
    Interface,
    Containment,
    DataType,
    Enumeration,
    EnumerationLiteral,
    Feature,
    Classifier,
    Link,
    LanguageEntity,
    PrimitiveType,
    Property,
    Reference,
    type INamed,
    type IKeyed,
    Language,
} from "../../language/gen/index.js";

/**
 * SeparatorType is used to unparse lists.
 * NONE means only space(s) between the elements.
 * Terminator means that every element is terminated with a certain string.
 * Separator means that in between elements a certain string is placed.
 */
enum SeparatorType {
    NONE = "NONE",
    Terminator = "Terminator",
    Separator = "Separator",
    Initiator = "Initiator",
}

/**
 * Class LionCore_M3ModelUnitWriter provides methods to return a string representation of an instance of
 * elements of language LionCore_M3.
 * It is, amongst others, used to create error messages in the validator.
 */
export class LionCore_M3ModelUnitWriter implements FreWriter {
    output: string[] = []; // stores the result, one line per array element
    currentLine: number = 0; // keeps track of the element in 'output' that we are working on

    /**
     * Returns a string representation of 'node'.
     * If 'short' is present and true, then a single-line result will be given.
     * Otherwise, the result is always a multi-line string.
     * Note that the single-line-string cannot be parsed into a correct model.
     *
     * @param node
     * @param startIndent
     * @param short
     */
    public writeToString(node: FreNode, startIndent?: number, short?: boolean): string {
        this.writeToLines(node, startIndent, short);
        return `${this.output
            .map((line) => `${line.trimEnd()}`)
            .join("\n")
            .trimEnd()}`;
    }

    /**
     * Returns a string representation of 'node', divided into an array of strings,
     * each of which contain a single line (without newline).
     * If 'short' is present and true, then a single-line result will be given.
     * Otherwise, the result is always a multi-line string.
     *
     * @param node
     * @param startIndent
     * @param short
     */
    public writeToLines(node: FreNode, startIndent?: number, short?: boolean): string[] {
        // set default for optional parameters
        if (startIndent === undefined) {
            startIndent = 0;
        }
        if (short === undefined) {
            short = false;
        }

        // make sure the global variables are reset
        this.output = [];
        this.currentLine = 0;

        // begin the unparsing with an indent if asked for
        let indentString: string = "";
        for (let _i = 0; _i < startIndent; _i++) {
            indentString += " ";
        }
        this.output[this.currentLine] = indentString;

        // do the actual work
        this.unparse(node, short);
        return this.output;
    }

    /**
     * Returns the name of 'node' if it has one, else returns
     * a short unparsing of 'node'.
     * Used by the validator to produce readable error messages.
     *
     * @param node
     */
    public writeNameOnly(node: FreNode | undefined): string {
        if (isNullOrUndefined(node)) {
            return "";
        }

        if (node instanceof Language) {
            return node.name;
        } else if (node instanceof Annotation) {
            return node.name;
        } else if (node instanceof Concept) {
            return node.name;
        } else if (node instanceof Interface) {
            return node.name;
        } else if (node instanceof Containment) {
            return node.name;
        } else if (node instanceof DataType) {
            return node.name;
        } else if (node instanceof Enumeration) {
            return node.name;
        } else if (node instanceof EnumerationLiteral) {
            return node.name;
        } else if (node instanceof Feature) {
            return node.name;
        } else if (node instanceof Classifier) {
            return node.name;
        } else if (node instanceof Link) {
            return node.name;
        } else if (node instanceof LanguageEntity) {
            return node.name;
        } else if (node instanceof PrimitiveType) {
            return node.name;
        } else if (node instanceof Property) {
            return node.name;
        } else if (node instanceof Reference) {
            return node.name;
        } else {
            // make sure the global variables are reset
            this.output = [];
            this.currentLine = 0;
            // do not care about indent, we just need a single line
            this.output[this.currentLine] = "";
            // do the actual work
            this.unparse(node, true);
            return this.output[0].trimEnd();
        }
    }

    private unparse(node: FreNode, short: boolean) {
        if (isNullOrUndefined(node)) {
            return;
        }
        switch (node.freLanguageConcept()) {
            case "Node":
                this.unparseNode(node as Node, short);
                break;
            case "Annotation":
                this.unparseAnnotation(node as Annotation, short);
                break;
            case "Concept":
                this.unparseConcept(node as Concept, short);
                break;
            case "Interface":
                this.unparseInterface(node as Interface, short);
                break;
            case "Containment":
                this.unparseContainment(node as Containment, short);
                break;
            case "DataType":
                this.unparseDataType(node as DataType, short);
                break;
            case "Enumeration":
                this.unparseEnumeration(node as Enumeration, short);
                break;
            case "EnumerationLiteral":
                this.unparseEnumerationLiteral(node as EnumerationLiteral, short);
                break;
            case "Feature":
                this.unparseFeature(node as Feature, short);
                break;
            case "Classifier":
                this.unparseClassifier(node as Classifier, short);
                break;
            case "Link":
                this.unparseLink(node as Link, short);
                break;
            case "LanguageEntity":
                this.unparseLanguageEntity(node as LanguageEntity, short);
                break;
            case "PrimitiveType":
                this.unparsePrimitiveType(node as PrimitiveType, short);
                break;
            case "Property":
                this.unparseProperty(node as Property, short);
                break;
            case "Reference":
                this.unparseReference(node as Reference, short);
                break;
            case "Language":
                this.unparseLanguage(node as Language, short);
                break;
            case "INamed":
                this.unparseINamed(node as INamed, short);
                break;
            case "IKeyed":
                this.unparseIKeyed(node as IKeyed, short);
                break;
            default:
                console.error("Cannot unparse a " + node.freLanguageConcept());
        }
    }

    /**
     * Unparsing of 'Language' according to projection 'default'.
     */
    private unparseLanguage(node: Language, short: boolean) {
        const blockIndent = this.output[this.currentLine].length;
        // do the first line
        this.output[this.currentLine] += `language `;
        this.output[this.currentLine] += `\`${node.name}\` ` /* 3 */;
        this.output[this.currentLine] += `key `;
        this.output[this.currentLine] += notNullOrUndefined(node.key) ? `"${node.key}" ` : `""` /* 5 */;

        if (!short) {
            // do the rest of the lines as well
            this.newlineAndIndentation(blockIndent + 0);
            this.output[this.currentLine] += `version `;
            this.output[this.currentLine] += notNullOrUndefined(node.version) ? `"${node.version}" ` : `""` /* 5 */;
            this.newlineAndIndentation(blockIndent + 0);
            this.output[this.currentLine] += `depends on languages: `;
            this._unparseReferenceList(node.dependsOn, "", SeparatorType.Separator, true, this.output[this.currentLine].length, short);
            this.newlineAndIndentation(blockIndent + 0);
            this.output[this.currentLine] += "";
            ("");
            this.newlineAndIndentation(blockIndent + 0);
        }
    }

    /**
     * Unparsing of 'Concept' according to projection 'default'.
     */
    private unparseConcept(node: Concept, short: boolean) {
        const blockIndent = this.output[this.currentLine].length;
        // do the first line
        this.output[this.currentLine] += `concept `;
        this.output[this.currentLine] += `\`${node.name}\` ` /* 3 */;

        if (!short) {
            // do the rest of the lines as well
            this.newlineAndIndentation(blockIndent + 4);
            this.output[this.currentLine] += `key: `;
            this.output[this.currentLine] += notNullOrUndefined(node.key) ? `"${node.key}" ` : `""` /* 5 */;
            this.newlineAndIndentation(blockIndent + 4);
            this._unparseList(
                node.features,
                "",
                SeparatorType.Separator,
                true,
                this.output[this.currentLine].length,
                short,
                (node, short) => this.unparse(node, short),
            );
        }
    }

    /**
     * Unparsing of 'Interface' according to projection 'default'.
     */
    private unparseInterface(node: Interface, short: boolean) {
        const blockIndent = this.output[this.currentLine].length;
        // do the first line
        this.output[this.currentLine] += `interface `;
        this.output[this.currentLine] += `\`${node.name}\` ` /* 3 */;
        if (notNullOrUndefined(node.extends && node.extends.length > 0)) {
            this.output[this.currentLine] += `extends `;
            this._unparseReferenceList(node.extends, "", SeparatorType.Separator, true, this.output[this.currentLine].length, short);
        }
        this.output[this.currentLine] += `\{ `;

        if (!short) {
            // do the rest of the lines as well
            this.newlineAndIndentation(blockIndent + 4);
            this.output[this.currentLine] += `key: `;
            this.output[this.currentLine] += notNullOrUndefined(node.key) ? `"${node.key}" ` : `""` /* 5 */;
            this.newlineAndIndentation(blockIndent + 4);
            this._unparseList(
                node.features,
                "",
                SeparatorType.Separator,
                true,
                this.output[this.currentLine].length,
                short,
                (node, short) => this.unparse(node, short),
            );
        }
    }

    /**
     * Unparsing of 'Feature' according to projection 'default'.
     */
    private unparseFeature(node: Feature, short: boolean) {
        const blockIndent = this.output[this.currentLine].length;
        // do the first line
        this.output[this.currentLine] += `name: `;
        this.output[this.currentLine] += `\`${node.name}\` ` /* 3 */;

        if (!short) {
            // do the rest of the lines as well
            this.newlineAndIndentation(blockIndent + 0);
            this.output[this.currentLine] += `key: `;
            this.output[this.currentLine] += notNullOrUndefined(node.key) ? `"${node.key}" ` : `""` /* 5 */;
            this.newlineAndIndentation(blockIndent + 0);
            this.output[this.currentLine] += `optional: `;
            this.output[this.currentLine] += notNullOrUndefined(node.optional) ? `${node.optional} ` : `<no-value> ` /* 1a */;
        }
    }

    /**
     * Unparsing of 'Property' according to projection 'default'.
     */
    private unparseProperty(node: Property, short: boolean) {
        if (node.optional) {
            this.output[this.currentLine] += `optional `;
        } else {
            this.output[this.currentLine] += notNullOrUndefined(node.optional) ? `mandatory ` : `<no-value> `; /* 1b */
        }
        this.output[this.currentLine] += `property `;
        this.output[this.currentLine] += `\`${node.name}\` ` /* 3 */;
        this.output[this.currentLine] += `( `;
        this.output[this.currentLine] += notNullOrUndefined(node.key) ? `"${node.key}" ` : `""` /* 5 */;
        this.output[this.currentLine] += `): `;
        this._unparseReference(node.type, short);
    }

    /**
     * Unparsing of 'Reference' according to projection 'default'.
     */
    private unparseReference(node: Reference, short: boolean) {
        if (node.optional) {
            this.output[this.currentLine] += `optional `;
        } else {
            this.output[this.currentLine] += notNullOrUndefined(node.optional) ? `mandatory ` : `<no-value> `; /* 1b */
        }
        this.output[this.currentLine] += `reference `;
        this.output[this.currentLine] += `\`${node.name}\` ` /* 3 */;
        this.output[this.currentLine] += `( `;
        this.output[this.currentLine] += notNullOrUndefined(node.key) ? `"${node.key}" ` : `""` /* 5 */;
        this.output[this.currentLine] += `): `;
        this._unparseReference(node.type, short);
        if (node.multiple) {
            this.output[this.currentLine] += `* `;
        } else {
            this.output[this.currentLine] += notNullOrUndefined(node.multiple) ? `1 ` : `<no-value> `; /* 1b */
        }
    }

    /**
     * Unparsing of 'Containment' according to projection 'default'.
     */
    private unparseContainment(node: Containment, short: boolean) {
        if (node.optional) {
            this.output[this.currentLine] += `optional `;
        } else {
            this.output[this.currentLine] += notNullOrUndefined(node.optional) ? `mandatory ` : `<no-value> `; /* 1b */
        }
        this.output[this.currentLine] += `containment `;
        this.output[this.currentLine] += `\`${node.name}\` ` /* 3 */;
        this.output[this.currentLine] += `( `;
        this.output[this.currentLine] += notNullOrUndefined(node.key) ? `"${node.key}" ` : `""` /* 5 */;
        this.output[this.currentLine] += `): `;
        this._unparseReference(node.type, short);
        if (node.multiple) {
            this.output[this.currentLine] += `* `;
        } else {
            this.output[this.currentLine] += notNullOrUndefined(node.multiple) ? `1 ` : `<no-value> `; /* 1b */
        }
    }

    /**
     * Unparsing of 'Annotation' according to projection 'default'.
     */
    private unparseAnnotation(node: Annotation, short: boolean) {
        const blockIndent = this.output[this.currentLine].length;
        // do the first line
        this.output[this.currentLine] += `annotation `;
        this.output[this.currentLine] += `\`${node.name}\` ` /* 3 */;
        this.output[this.currentLine] += `annotates `;
        if (notNullOrUndefined(node.annotates)) {
            this._unparseReference(node.annotates, short);
        }
        if (notNullOrUndefined(node.extends)) {
            this.output[this.currentLine] += `extends `;
            this._unparseReference(node.extends, short);
        }
        if (notNullOrUndefined(node.implements && node.implements.length > 0)) {
            this.output[this.currentLine] += `implements `;
            this._unparseReferenceList(node.implements, "", SeparatorType.Separator, true, this.output[this.currentLine].length, short);
        }
        this.output[this.currentLine] += `\{ `;

        if (!short) {
            // do the rest of the lines as well
            this.newlineAndIndentation(blockIndent + 4);
            this._unparseList(
                node.features,
                "",
                SeparatorType.Separator,
                true,
                this.output[this.currentLine].length,
                short,
                (node, short) => this.unparse(node, short),
            );
        }
    }

    /**
     * Unparsing of 'Enumeration' according to projection 'default'.
     */
    private unparseEnumeration(node: Enumeration, short: boolean) {
        const blockIndent = this.output[this.currentLine].length;
        // do the first line
        this.output[this.currentLine] += `Enumeration `;
        this.output[this.currentLine] += `\`${node.name}\` ` /* 3 */;
        this.output[this.currentLine] += `\{ `;

        if (!short) {
            // do the rest of the lines as well
            this.newlineAndIndentation(blockIndent + 4);
            this.output[this.currentLine] += `key `;
            this.output[this.currentLine] += notNullOrUndefined(node.key) ? `"${node.key}" ` : `""` /* 5 */;
            this.newlineAndIndentation(blockIndent + 4);
            this.output[this.currentLine] += `literals `;
            this.newlineAndIndentation(blockIndent + 8);
            this._unparseList(
                node.literals,
                "",
                SeparatorType.Separator,
                true,
                this.output[this.currentLine].length,
                short,
                (node, short) => this.unparse(node, short),
            );
            this.newlineAndIndentation(blockIndent + 0);
            this.output[this.currentLine] += `} `;
        }
    }

    /**
     * Unparsing of 'EnumerationLiteral' according to projection 'default'.
     */
    private unparseEnumerationLiteral(node: EnumerationLiteral, short: boolean) {
        const blockIndent = this.output[this.currentLine].length;
        // do the first line
        this.output[this.currentLine] += `EnumerationLiteral `;
        this.output[this.currentLine] += `\`${node.name}\` ` /* 3 */;
        this.output[this.currentLine] += `\{ `;

        if (!short) {
            // do the rest of the lines as well
            this.newlineAndIndentation(blockIndent + 4);
            this.output[this.currentLine] += `key `;
            this.output[this.currentLine] += notNullOrUndefined(node.key) ? `"${node.key}" ` : `""` /* 5 */;
            this.newlineAndIndentation(blockIndent + 0);
            this.output[this.currentLine] += `} `;
        }
    }

    /**
     * Unparsing of 'PrimitiveType' according to projection 'default'.
     */
    private unparsePrimitiveType(node: PrimitiveType, short: boolean) {
        const blockIndent = this.output[this.currentLine].length;
        // do the first line
        this.output[this.currentLine] += `PrimitiveType `;
        this.output[this.currentLine] += `\`${node.name}\` ` /* 3 */;
        this.output[this.currentLine] += `\{ `;

        if (!short) {
            // do the rest of the lines as well
            this.newlineAndIndentation(blockIndent + 4);
            this.output[this.currentLine] += `key `;
            this.output[this.currentLine] += notNullOrUndefined(node.key) ? `"${node.key}" ` : `""` /* 5 */;
            this.newlineAndIndentation(blockIndent + 0);
            this.output[this.currentLine] += `} `;
        }
    }

    /**
     * The abstract concept 'Node' is not unparsed.
     */
    private unparseNode(node: Node, short: boolean) {
        throw new Error("Method unparseNode should be implemented by its (concrete) subclasses.");
    }
    /**
     * The abstract concept 'DataType' is not unparsed.
     */
    private unparseDataType(node: DataType, short: boolean) {
        throw new Error("Method unparseDataType should be implemented by its (concrete) subclasses.");
    }
    /**
     * The abstract concept 'Classifier' is not unparsed.
     */
    private unparseClassifier(node: Classifier, short: boolean) {
        throw new Error("Method unparseClassifier should be implemented by its (concrete) subclasses.");
    }
    /**
     * The abstract concept 'Link' is not unparsed.
     */
    private unparseLink(node: Link, short: boolean) {
        throw new Error("Method unparseLink should be implemented by its (concrete) subclasses.");
    }
    /**
     * The abstract concept 'LanguageEntity' is not unparsed.
     */
    private unparseLanguageEntity(node: LanguageEntity, short: boolean) {
        throw new Error("Method unparseLanguageEntity should be implemented by its (concrete) subclasses.");
    }
    /**
     * The interface 'INamed' is not unparsed.
     */
    private unparseINamed(node: FreNode, short: boolean) {
        throw new Error("Method unparseINamed should be implemented by the classes that implement it.");
    }
    /**
     * The interface 'IKeyed' is not unparsed.
     */
    private unparseIKeyed(node: FreNode, short: boolean) {
        throw new Error("Method unparseIKeyed should be implemented by the classes that implement it.");
    }

    /**
     *
     */
    private _unparseReference(node: FreNodeReference<FreNamedNode>, short: boolean) {
        if (notNullOrUndefined(node)) {
            const type: FreNamedNode = node?.referred;
            if (notNullOrUndefined(type)) {
                this.output[this.currentLine] += node.pathname.map((s) => "`" + s + "`").join(".") + " ";
            } else {
                this.output[this.currentLine] += node.pathname.map((s) => "`" + s + "`").join(".") + " ";
            }
        }
    }

    /**
     * Adds a string representation of 'list' to the 'output', using 'sepText' , and 'sepType' to include either a separator string
     * or a terminator string. Param 'vertical' indicates whether the list should be represented vertically or horizontally.
     * If 'short' is false, then a multi-line result will be given. Otherwise, only one single-line string is added.
     * @param list
     * @param sepText
     * @param sepType
     * @param vertical
     * @param indent
     * @param short
     * @param method
     * @private
     */
    private _unparseList(
        list: FreNode[],
        sepText: string,
        sepType: SeparatorType,
        vertical: boolean,
        indent: number,
        short: boolean,
        method: (node: FreNode, short: boolean) => void,
    ) {
        list.forEach((listElem, index) => {
            const isLastInList: boolean = index === list.length - 1;
            this.doInitiator(sepText, sepType);
            method(listElem, short);
            this.doSeparatorOrTerminatorAndNewline(sepType, isLastInList, sepText, vertical, short, indent);
        });
    }

    /**
     * Adds a string representation of a list of references, where every reference
     * is replaced by the name of its referred element. The use of params
     * 'sepText' and 'SepType' are equals to those in the private method _unparseList.
     * @param list
     * @param sepText
     * @param sepType
     * @param vertical
     * @param indent
     * @param short
     */
    private _unparseReferenceList(
        list: FreNodeReference<FreNamedNode>[],
        sepText: string,
        sepType: SeparatorType,
        vertical: boolean,
        indent: number,
        short: boolean,
    ) {
        list.forEach((listElem, index) => {
            const isLastInList: boolean = index === list.length - 1;
            this.doInitiator(sepText, sepType);
            this._unparseReference(listElem, short);
            this.doSeparatorOrTerminatorAndNewline(sepType, isLastInList, sepText, vertical, short, indent);
        });
    }

    /**
     * Adds a string representation of 'list' to the 'output', using 'sepText' , and 'sepType' to include either a separator string
     * or a terminator string. Param 'vertical' indicates whether the list should be represented vertically or horizontally.
     * If 'short' is false, then a multi-line result will be given. Otherwise, only one single-line string is added.
     * @param list
     * @param isIdentifier indicates whether the value should be surrounded with double quotes
     * @param sepText
     * @param sepType
     * @param vertical
     * @param indent
     * @param short
     */
    private _unparseListOfPrimitiveValues(
        list: (string | number | boolean)[],
        isIdentifier: boolean,
        sepText: string,
        sepType: SeparatorType,
        vertical: boolean,
        indent: number,
        short: boolean,
    ) {
        if (notNullOrUndefined(list)) {
            list.forEach((listElem, index) => {
                const isLastInList: boolean = index === list.length - 1;
                this.doInitiator(sepText, sepType);
                if (typeof listElem === "string" && !isIdentifier) {
                    this.output[this.currentLine] += `"${listElem}"`;
                } else if (typeof listElem === "string" && isIdentifier) {
                    this.output[this.currentLine] += `\`${listElem}\``;
                } else {
                    this.output[this.currentLine] += `${listElem}`;
                }
                this.doSeparatorOrTerminatorAndNewline(sepType, isLastInList, sepText, vertical, short, indent);
            });
        }
    }

    /**
     * Adds a separator, terminator, or initiator text (followed or preceded by a newline and the right amount of indentation)
     * to the output, depending on the parameters.
     * @param sepType
     * @param isLastInList
     * @param sepText
     * @param vertical
     * @param short
     * @param indent
     */
    private doSeparatorOrTerminatorAndNewline(
        sepType: SeparatorType,
        isLastInList: boolean,
        sepText: string,
        vertical: boolean,
        short: boolean,
        indent: number,
    ) {
        // first eliminate any whitespace at the end of the line
        this.output[this.currentLine] = this.output[this.currentLine].trimEnd();

        if (!vertical && (!sepText || sepText.length == 0)) {
            // at least separate the items by a space to avoid things
            // like "IntegerFunction", which should be "Integer Function"
            sepText = " ";
        }

        // then add the right separator or terminator
        switch (sepType) {
            case SeparatorType.Separator: {
                if (!isLastInList) {
                    this.output[this.currentLine] += sepText;
                }
                break;
            }
            case SeparatorType.Terminator: {
                this.output[this.currentLine] += sepText;
                break;
            }
            case SeparatorType.Initiator: {
                break;
            }
            case SeparatorType.NONE: {
                if (!vertical) {
                    // at least separate the items by a space to avoid things
                    // like "IntegerFunction", which should be "Integer Function"
                    this.output[this.currentLine] += " ";
                }
                break;
            }
        }

        // then add newline and indentation
        if (vertical && !isLastInList) {
            if (!short) {
                this.newlineAndIndentation(indent);
            } else {
                // stop after 1 line
                // note that the following cannot be parsed
                this.output[this.currentLine] += ` ...`;
            }
        } else if (isLastInList) {
            // end with a space to avoid things
            // like "666after", which should be "666 after"
            if (this.output[this.currentLine][this.output[this.currentLine].length - 1] !== " ") {
                this.output[this.currentLine] += ` `;
            }
        }
    }

    /**
     * Makes a new entry in the 'output' array
     * and adds the indentation of 'number' spaces
     * to the new entry/line.
     * @param indent
     */
    private newlineAndIndentation(indent: number) {
        this.currentLine += 1;
        let indentation: string = "";
        for (let _i = 0; _i < indent; _i++) {
            indentation += " ";
        }
        this.output[this.currentLine] = indentation;
    }

    /**
     * Adds the 'initiator' text
     * @param sepText
     * @param sepType
     * @private
     */
    private doInitiator(sepText: string, sepType: SeparatorType) {
        if (sepType === SeparatorType.Initiator) {
            const nrOfWhiteSpaces = this.output[this.currentLine].split("").filter((char) => /\s/.test(char)).length;
            const onlyIndentation = this.output[this.currentLine].length === nrOfWhiteSpaces;
            if (onlyIndentation) {
                this.output[this.currentLine] += sepText;
            } else {
                // add space before initiator, if it is not the first on the line
                this.output[this.currentLine] += " " + sepText;
            }
        }
    }
}

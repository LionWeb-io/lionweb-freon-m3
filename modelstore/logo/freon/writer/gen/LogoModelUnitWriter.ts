// Generated by the Freon Language Generator.
// TEMPLATE WriterTemplate.generateUnparser(...)

import { type FreNamedNode, FreNodeReference, type FreWriter, type FreNode, isNullOrUndefined, notNullOrUndefined } from "@freon4dsl/core";
import {
    Node,
    MoveCommand,
    List,
    SetPos,
    SetHeading,
    HomeCommand,
    Repeat,
    If,
    Procedure,
    Parameter,
    Direction,
    type INamed,
    type ICommand,
    type IExpression,
    Program,
    Library,
} from "../../language/gen/index.js";

/**
 * SeparatorType is used to unparse lists.
 * NONE means only space(s) between the elements.
 * Terminator means that every element is terminated with a certain string.
 * Separator means that in between elements a certain string is placed.
 */
enum SeparatorType {
    NONE = "NONE",
    Terminator = "Terminator",
    Separator = "Separator",
    Initiator = "Initiator",
}

/**
 * Class LogoModelUnitWriter provides methods to return a string representation of an instance of
 * elements of language logo.
 * It is, amongst others, used to create error messages in the validator.
 */
export class LogoModelUnitWriter implements FreWriter {
    output: string[] = []; // stores the result, one line per array element
    currentLine: number = 0; // keeps track of the element in 'output' that we are working on

    /**
     * Returns a string representation of 'node'.
     * If 'short' is present and true, then a single-line result will be given.
     * Otherwise, the result is always a multi-line string.
     * Note that the single-line-string cannot be parsed into a correct model.
     *
     * @param node
     * @param startIndent
     * @param short
     */
    public writeToString(node: FreNode, startIndent?: number, short?: boolean): string {
        this.writeToLines(node, startIndent, short);
        return `${this.output
            .map((line) => `${line.trimEnd()}`)
            .join("\n")
            .trimEnd()}`;
    }

    /**
     * Returns a string representation of 'node', divided into an array of strings,
     * each of which contain a single line (without newline).
     * If 'short' is present and true, then a single-line result will be given.
     * Otherwise, the result is always a multi-line string.
     *
     * @param node
     * @param startIndent
     * @param short
     */
    public writeToLines(node: FreNode, startIndent?: number, short?: boolean): string[] {
        // set default for optional parameters
        if (startIndent === undefined) {
            startIndent = 0;
        }
        if (short === undefined) {
            short = false;
        }

        // make sure the global variables are reset
        this.output = [];
        this.currentLine = 0;

        // begin the unparsing with an indent if asked for
        let indentString: string = "";
        for (let _i = 0; _i < startIndent; _i++) {
            indentString += " ";
        }
        this.output[this.currentLine] = indentString;

        // do the actual work
        this.unparse(node, short);
        return this.output;
    }

    /**
     * Returns the name of 'node' if it has one, else returns
     * a short unparsing of 'node'.
     * Used by the validator to produce readable error messages.
     *
     * @param node
     */
    public writeNameOnly(node: FreNode | undefined): string {
        if (isNullOrUndefined(node)) {
            return "";
        }

        if (node instanceof Program) {
            return node.name;
        } else if (node instanceof Library) {
            return node.name;
        } else if (node instanceof MoveCommand) {
            return node.name;
        } else if (node instanceof SetPos) {
            return node.name;
        } else if (node instanceof SetHeading) {
            return node.name;
        } else if (node instanceof HomeCommand) {
            return node.name;
        } else if (node instanceof Procedure) {
            return node.name;
        } else if (node instanceof Parameter) {
            return node.name;
        } else if (node instanceof Direction) {
            return node.name;
        } else {
            // make sure the global variables are reset
            this.output = [];
            this.currentLine = 0;
            // do not care about indent, we just need a single line
            this.output[this.currentLine] = "";
            // do the actual work
            this.unparse(node, true);
            return this.output[0].trimEnd();
        }
    }

    private unparse(node: FreNode, short: boolean) {
        if (isNullOrUndefined(node)) {
            return;
        }
        switch (node.freLanguageConcept()) {
            case "Node":
                this.unparseNode(node as Node, short);
                break;
            case "MoveCommand":
                this.unparseMoveCommand(node as MoveCommand, short);
                break;
            case "List":
                this.unparseList(node as List, short);
                break;
            case "SetPos":
                this.unparseSetPos(node as SetPos, short);
                break;
            case "SetHeading":
                this.unparseSetHeading(node as SetHeading, short);
                break;
            case "HomeCommand":
                this.unparseHomeCommand(node as HomeCommand, short);
                break;
            case "Repeat":
                this.unparseRepeat(node as Repeat, short);
                break;
            case "If":
                this.unparseIf(node as If, short);
                break;
            case "Procedure":
                this.unparseProcedure(node as Procedure, short);
                break;
            case "Parameter":
                this.unparseParameter(node as Parameter, short);
                break;
            case "Direction":
                this.unparseDirection(node as Direction, short);
                break;
            case "Program":
                this.unparseProgram(node as Program, short);
                break;
            case "Library":
                this.unparseLibrary(node as Library, short);
                break;
            case "INamed":
                this.unparseINamed(node as INamed, short);
                break;
            case "ICommand":
                this.unparseICommand(node as ICommand, short);
                break;
            case "IExpression":
                this.unparseIExpression(node as IExpression, short);
                break;
            default:
                console.error("Cannot unparse a " + node.freLanguageConcept());
        }
    }

    /**
     * Unparsing of 'MoveCommand' according to projection 'default'.
     */
    private unparseMoveCommand(node: MoveCommand, short: boolean) {
        const blockIndent = this.output[this.currentLine].length;
        // do the first line
        this.output[this.currentLine] += `MoveCommand `;
        this.output[this.currentLine] += `\`${node.name}\` ` /* 3 */;
        this.output[this.currentLine] += `\{ `;

        if (!short) {
            // do the rest of the lines as well
            this.newlineAndIndentation(blockIndent + 4);
            this.output[this.currentLine] += `distance `;
            this.output[this.currentLine] += notNullOrUndefined(node.distance) ? `${node.distance} ` : `<no-value> ` /* 2 */;
            this.newlineAndIndentation(blockIndent + 4);
            this.output[this.currentLine] += `direction `;
            this._unparseReference(node.direction, short);
            this.newlineAndIndentation(blockIndent + 0);
            this.output[this.currentLine] += `} `;
        }
    }

    /**
     * Unparsing of 'List' according to projection 'default'.
     */
    private unparseList(node: List, short: boolean) {
        const blockIndent = this.output[this.currentLine].length;
        // do the first line
        this.output[this.currentLine] += `List `;
        this.output[this.currentLine] += `\{ `;

        if (!short) {
            // do the rest of the lines as well
            this.newlineAndIndentation(blockIndent + 4);
            this.output[this.currentLine] += `commands `;
            this.newlineAndIndentation(blockIndent + 8);
            this._unparseList(
                node.commands,
                "",
                SeparatorType.Separator,
                true,
                this.output[this.currentLine].length,
                short,
                (node, short) => this.unparse(node, short),
            );
            this.newlineAndIndentation(blockIndent + 0);
            this.output[this.currentLine] += `} `;
        }
    }

    /**
     * Unparsing of 'SetPos' according to projection 'default'.
     */
    private unparseSetPos(node: SetPos, short: boolean) {
        const blockIndent = this.output[this.currentLine].length;
        // do the first line
        this.output[this.currentLine] += `SetPos `;
        this.output[this.currentLine] += `\`${node.name}\` ` /* 3 */;
        this.output[this.currentLine] += `\{ `;

        if (!short) {
            // do the rest of the lines as well
            this.newlineAndIndentation(blockIndent + 4);
            this.output[this.currentLine] += `x `;
            this.output[this.currentLine] += notNullOrUndefined(node.x) ? `${node.x} ` : `<no-value> ` /* 2 */;
            this.newlineAndIndentation(blockIndent + 4);
            this.output[this.currentLine] += `y `;
            this.output[this.currentLine] += notNullOrUndefined(node.y) ? `${node.y} ` : `<no-value> ` /* 2 */;
            this.newlineAndIndentation(blockIndent + 0);
            this.output[this.currentLine] += `} `;
        }
    }

    /**
     * Unparsing of 'SetHeading' according to projection 'default'.
     */
    private unparseSetHeading(node: SetHeading, short: boolean) {
        const blockIndent = this.output[this.currentLine].length;
        // do the first line
        this.output[this.currentLine] += `SetHeading `;
        this.output[this.currentLine] += `\`${node.name}\` ` /* 3 */;
        this.output[this.currentLine] += `\{ `;

        if (!short) {
            // do the rest of the lines as well
            this.newlineAndIndentation(blockIndent + 4);
            this.output[this.currentLine] += `degrees `;
            this.output[this.currentLine] += notNullOrUndefined(node.degrees) ? `${node.degrees} ` : `<no-value> ` /* 2 */;
            this.newlineAndIndentation(blockIndent + 0);
            this.output[this.currentLine] += `} `;
        }
    }

    /**
     * Unparsing of 'HomeCommand' according to projection 'default'.
     */
    private unparseHomeCommand(node: HomeCommand, short: boolean) {
        const blockIndent = this.output[this.currentLine].length;
        // do the first line
        this.output[this.currentLine] += `HomeCommand `;
        this.output[this.currentLine] += `\`${node.name}\` ` /* 3 */;
        this.output[this.currentLine] += `\{ `;

        if (!short) {
            // do the rest of the lines as well
            this.newlineAndIndentation(blockIndent + 0);
            this.output[this.currentLine] += `} `;
        }
    }

    /**
     * Unparsing of 'Repeat' according to projection 'default'.
     */
    private unparseRepeat(node: Repeat, short: boolean) {
        const blockIndent = this.output[this.currentLine].length;
        // do the first line
        this.output[this.currentLine] += `Repeat `;
        this.output[this.currentLine] += `\{ `;

        if (!short) {
            // do the rest of the lines as well
            this.newlineAndIndentation(blockIndent + 4);
            this.output[this.currentLine] += `count `;
            this.output[this.currentLine] += notNullOrUndefined(node.count) ? `${node.count} ` : `<no-value> ` /* 2 */;
            this.newlineAndIndentation(blockIndent + 4);
            this.output[this.currentLine] += `list `;
            this.unparse(node.list, short);
            this.newlineAndIndentation(blockIndent + 0);
            this.output[this.currentLine] += `} `;
        }
    }

    /**
     * Unparsing of 'If' according to projection 'default'.
     */
    private unparseIf(node: If, short: boolean) {
        const blockIndent = this.output[this.currentLine].length;
        // do the first line
        this.output[this.currentLine] += `If `;
        this.output[this.currentLine] += `\{ `;

        if (!short) {
            // do the rest of the lines as well
            this.newlineAndIndentation(blockIndent + 4);
            this.output[this.currentLine] += `condition `;
            this.unparse(node.condition, short);
            this.newlineAndIndentation(blockIndent + 4);
            this.output[this.currentLine] += `ifTrue `;
            this.unparse(node.ifTrue, short);
            if (notNullOrUndefined(node.ifFalse)) {
                this.newlineAndIndentation(blockIndent + 0 + 4);
                this.output[this.currentLine] += `ifFalse `;
                this.unparse(node.ifFalse, short);
            }
            this.newlineAndIndentation(blockIndent + 0);
            this.output[this.currentLine] += `} `;
        }
    }

    /**
     * Unparsing of 'Procedure' according to projection 'default'.
     */
    private unparseProcedure(node: Procedure, short: boolean) {
        const blockIndent = this.output[this.currentLine].length;
        // do the first line
        this.output[this.currentLine] += `Procedure `;
        this.output[this.currentLine] += `\`${node.name}\` ` /* 3 */;
        this.output[this.currentLine] += `\{ `;

        if (!short) {
            // do the rest of the lines as well
            this.newlineAndIndentation(blockIndent + 4);
            this.output[this.currentLine] += `body `;
            this.unparse(node.body, short);
            this.newlineAndIndentation(blockIndent + 4);
            this.output[this.currentLine] += `paremeter `;
            this.unparse(node.paremeter, short);
            this.newlineAndIndentation(blockIndent + 0);
            this.output[this.currentLine] += `} `;
        }
    }

    /**
     * Unparsing of 'Parameter' according to projection 'default'.
     */
    private unparseParameter(node: Parameter, short: boolean) {
        const blockIndent = this.output[this.currentLine].length;
        // do the first line
        this.output[this.currentLine] += `Parameter `;
        this.output[this.currentLine] += `\`${node.name}\` ` /* 3 */;
        this.output[this.currentLine] += `\{ `;

        if (!short) {
            // do the rest of the lines as well
            this.newlineAndIndentation(blockIndent + 0);
            this.output[this.currentLine] += `} `;
        }
    }

    /**
     * Unparsing of 'Program' according to projection 'default'.
     */
    private unparseProgram(node: Program, short: boolean) {
        const blockIndent = this.output[this.currentLine].length;
        // do the first line
        this.output[this.currentLine] += `Program `;
        this.output[this.currentLine] += `\`${node.name}\` ` /* 3 */;
        this.output[this.currentLine] += `\{ `;

        if (!short) {
            // do the rest of the lines as well
            this.newlineAndIndentation(blockIndent + 4);
            this.output[this.currentLine] += `commands `;
            this.newlineAndIndentation(blockIndent + 8);
            this._unparseList(
                node.commands,
                "",
                SeparatorType.Separator,
                true,
                this.output[this.currentLine].length,
                short,
                (node, short) => this.unparse(node, short),
            );
            this.newlineAndIndentation(blockIndent + 0);
            this.output[this.currentLine] += `} `;
        }
    }

    /**
     * Unparsing of 'Library' according to projection 'default'.
     */
    private unparseLibrary(node: Library, short: boolean) {
        const blockIndent = this.output[this.currentLine].length;
        // do the first line
        this.output[this.currentLine] += `Library `;
        this.output[this.currentLine] += `\`${node.name}\` ` /* 3 */;
        this.output[this.currentLine] += `\{ `;

        if (!short) {
            // do the rest of the lines as well
            this.newlineAndIndentation(blockIndent + 4);
            this.output[this.currentLine] += `procedures `;
            this.unparse(node.procedures, short);
            this.newlineAndIndentation(blockIndent + 0);
            this.output[this.currentLine] += `} `;
        }
    }

    /**
     * Unparsing of 'ICommand' according to projection 'default'.
     */
    private unparseICommand(node: ICommand, short: boolean) {
        const blockIndent = this.output[this.currentLine].length;
        // do the first line
        this.output[this.currentLine] += `ICommand `;
        this.output[this.currentLine] += `\`${node.name}\` ` /* 3 */;
        this.output[this.currentLine] += `\{ `;

        if (!short) {
            // do the rest of the lines as well
            this.newlineAndIndentation(blockIndent + 0);
            this.output[this.currentLine] += `} `;
        }
    }

    /**
     * Unparsing of 'IExpression' according to projection 'default'.
     */
    private unparseIExpression(node: IExpression, short: boolean) {
        const blockIndent = this.output[this.currentLine].length;
        // do the first line
        this.output[this.currentLine] += `IExpression `;
        this.output[this.currentLine] += `\{ `;

        if (!short) {
            // do the rest of the lines as well
            this.newlineAndIndentation(blockIndent + 0);
            this.output[this.currentLine] += `} `;
        }
    }

    /**
     * The limited concept 'Direction' is unparsed as its name.
     */
    private unparseDirection(node: Direction, short: boolean) {
        if (notNullOrUndefined(node)) {
            this.output[this.currentLine] += "`" + node.name + "` " /* 9 */;
        }
    }
    /**
     * The abstract concept 'Node' is not unparsed.
     */
    private unparseNode(node: Node, short: boolean) {
        throw new Error("Method unparseNode should be implemented by its (concrete) subclasses.");
    }
    /**
     * The interface 'INamed' is not unparsed.
     */
    private unparseINamed(node: FreNode, short: boolean) {
        throw new Error("Method unparseINamed should be implemented by the classes that implement it.");
    }

    /**
     *
     */
    private _unparseReference(node: FreNodeReference<FreNamedNode>, short: boolean) {
        if (notNullOrUndefined(node)) {
            const type: FreNamedNode = node?.referred;
            if (notNullOrUndefined(type)) {
                if (type instanceof Direction) {
                    this.unparseDirection(type, short);
                } else {
                    this.output[this.currentLine] += node.pathname.map((s) => "`" + s + "`").join(".") + " ";
                }
            } else {
                this.output[this.currentLine] += node.pathname.map((s) => "`" + s + "`").join(".") + " ";
            }
        }
    }

    /**
     * Adds a string representation of 'list' to the 'output', using 'sepText' , and 'sepType' to include either a separator string
     * or a terminator string. Param 'vertical' indicates whether the list should be represented vertically or horizontally.
     * If 'short' is false, then a multi-line result will be given. Otherwise, only one single-line string is added.
     * @param list
     * @param sepText
     * @param sepType
     * @param vertical
     * @param indent
     * @param short
     * @param method
     * @private
     */
    private _unparseList(
        list: FreNode[],
        sepText: string,
        sepType: SeparatorType,
        vertical: boolean,
        indent: number,
        short: boolean,
        method: (node: FreNode, short: boolean) => void,
    ) {
        list.forEach((listElem, index) => {
            const isLastInList: boolean = index === list.length - 1;
            this.doInitiator(sepText, sepType);
            method(listElem, short);
            this.doSeparatorOrTerminatorAndNewline(sepType, isLastInList, sepText, vertical, short, indent);
        });
    }

    /**
     * Adds a string representation of a list of references, where every reference
     * is replaced by the name of its referred element. The use of params
     * 'sepText' and 'SepType' are equals to those in the private method _unparseList.
     * @param list
     * @param sepText
     * @param sepType
     * @param vertical
     * @param indent
     * @param short
     */
    private _unparseReferenceList(
        list: FreNodeReference<FreNamedNode>[],
        sepText: string,
        sepType: SeparatorType,
        vertical: boolean,
        indent: number,
        short: boolean,
    ) {
        list.forEach((listElem, index) => {
            const isLastInList: boolean = index === list.length - 1;
            this.doInitiator(sepText, sepType);
            this._unparseReference(listElem, short);
            this.doSeparatorOrTerminatorAndNewline(sepType, isLastInList, sepText, vertical, short, indent);
        });
    }

    /**
     * Adds a string representation of 'list' to the 'output', using 'sepText' , and 'sepType' to include either a separator string
     * or a terminator string. Param 'vertical' indicates whether the list should be represented vertically or horizontally.
     * If 'short' is false, then a multi-line result will be given. Otherwise, only one single-line string is added.
     * @param list
     * @param isIdentifier indicates whether the value should be surrounded with double quotes
     * @param sepText
     * @param sepType
     * @param vertical
     * @param indent
     * @param short
     */
    private _unparseListOfPrimitiveValues(
        list: (string | number | boolean)[],
        isIdentifier: boolean,
        sepText: string,
        sepType: SeparatorType,
        vertical: boolean,
        indent: number,
        short: boolean,
    ) {
        if (notNullOrUndefined(list)) {
            list.forEach((listElem, index) => {
                const isLastInList: boolean = index === list.length - 1;
                this.doInitiator(sepText, sepType);
                if (typeof listElem === "string" && !isIdentifier) {
                    this.output[this.currentLine] += `"${listElem}"`;
                } else if (typeof listElem === "string" && isIdentifier) {
                    this.output[this.currentLine] += `\`${listElem}\``;
                } else {
                    this.output[this.currentLine] += `${listElem}`;
                }
                this.doSeparatorOrTerminatorAndNewline(sepType, isLastInList, sepText, vertical, short, indent);
            });
        }
    }

    /**
     * Adds a separator, terminator, or initiator text (followed or preceded by a newline and the right amount of indentation)
     * to the output, depending on the parameters.
     * @param sepType
     * @param isLastInList
     * @param sepText
     * @param vertical
     * @param short
     * @param indent
     */
    private doSeparatorOrTerminatorAndNewline(
        sepType: SeparatorType,
        isLastInList: boolean,
        sepText: string,
        vertical: boolean,
        short: boolean,
        indent: number,
    ) {
        // first eliminate any whitespace at the end of the line
        this.output[this.currentLine] = this.output[this.currentLine].trimEnd();

        if (!vertical && (!sepText || sepText.length == 0)) {
            // at least separate the items by a space to avoid things
            // like "IntegerFunction", which should be "Integer Function"
            sepText = " ";
        }

        // then add the right separator or terminator
        switch (sepType) {
            case SeparatorType.Separator: {
                if (!isLastInList) {
                    this.output[this.currentLine] += sepText;
                }
                break;
            }
            case SeparatorType.Terminator: {
                this.output[this.currentLine] += sepText;
                break;
            }
            case SeparatorType.Initiator: {
                break;
            }
            case SeparatorType.NONE: {
                if (!vertical) {
                    // at least separate the items by a space to avoid things
                    // like "IntegerFunction", which should be "Integer Function"
                    this.output[this.currentLine] += " ";
                }
                break;
            }
        }

        // then add newline and indentation
        if (vertical && !isLastInList) {
            if (!short) {
                this.newlineAndIndentation(indent);
            } else {
                // stop after 1 line
                // note that the following cannot be parsed
                this.output[this.currentLine] += ` ...`;
            }
        } else if (isLastInList) {
            // end with a space to avoid things
            // like "666after", which should be "666 after"
            if (this.output[this.currentLine][this.output[this.currentLine].length - 1] !== " ") {
                this.output[this.currentLine] += ` `;
            }
        }
    }

    /**
     * Makes a new entry in the 'output' array
     * and adds the indentation of 'number' spaces
     * to the new entry/line.
     * @param indent
     */
    private newlineAndIndentation(indent: number) {
        this.currentLine += 1;
        let indentation: string = "";
        for (let _i = 0; _i < indent; _i++) {
            indentation += " ";
        }
        this.output[this.currentLine] = indentation;
    }

    /**
     * Adds the 'initiator' text
     * @param sepText
     * @param sepType
     * @private
     */
    private doInitiator(sepText: string, sepType: SeparatorType) {
        if (sepType === SeparatorType.Initiator) {
            const nrOfWhiteSpaces = this.output[this.currentLine].split("").filter((char) => /\s/.test(char)).length;
            const onlyIndentation = this.output[this.currentLine].length === nrOfWhiteSpaces;
            if (onlyIndentation) {
                this.output[this.currentLine] += sepText;
            } else {
                // add space before initiator, if it is not the first on the line
                this.output[this.currentLine] += " " + sepText;
            }
        }
    }
}

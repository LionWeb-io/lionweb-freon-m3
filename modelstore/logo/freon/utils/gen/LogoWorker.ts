// Generated by the Freon Language Generator.
// TEMPLATE: WorkerInterfaceTemplate.generateWorkerInterface(...)

import {
    Node,
    MoveCommand,
    List,
    SetPos,
    SetHeading,
    HomeCommand,
    Repeat,
    If,
    Procedure,
    Parameter,
    Direction,
    Program,
    Library,
    Logo,
} from "../../language/gen/index.js";

/**
 * Interface LogoWorker implements the extended visitor pattern of instances of language logo.
 * Class LogoWalker implements the traversal of the model tree, classes that implement this interface
 * are responsible for the actual work being done on the nodes of the tree.
 * Every node is visited twice, once before the visit of its children, and once after this visit.
 */
export interface LogoWorker {
    execBeforeLogo(node: Logo): boolean;
    execAfterLogo(node: Logo): boolean;

    execBeforeProgram(node: Program): boolean;
    execAfterProgram(node: Program): boolean;

    execBeforeLibrary(node: Library): boolean;
    execAfterLibrary(node: Library): boolean;

    execBeforeNode(node: Node): boolean;
    execAfterNode(node: Node): boolean;

    execBeforeMoveCommand(node: MoveCommand): boolean;
    execAfterMoveCommand(node: MoveCommand): boolean;

    execBeforeList(node: List): boolean;
    execAfterList(node: List): boolean;

    execBeforeSetPos(node: SetPos): boolean;
    execAfterSetPos(node: SetPos): boolean;

    execBeforeSetHeading(node: SetHeading): boolean;
    execAfterSetHeading(node: SetHeading): boolean;

    execBeforeHomeCommand(node: HomeCommand): boolean;
    execAfterHomeCommand(node: HomeCommand): boolean;

    execBeforeRepeat(node: Repeat): boolean;
    execAfterRepeat(node: Repeat): boolean;

    execBeforeIf(node: If): boolean;
    execAfterIf(node: If): boolean;

    execBeforeProcedure(node: Procedure): boolean;
    execAfterProcedure(node: Procedure): boolean;

    execBeforeParameter(node: Parameter): boolean;
    execAfterParameter(node: Parameter): boolean;

    execBeforeDirection(node: Direction): boolean;
    execAfterDirection(node: Direction): boolean;
}

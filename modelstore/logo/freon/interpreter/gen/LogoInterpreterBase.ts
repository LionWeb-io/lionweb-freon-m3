// Generated by the Freon Language Generator.
// TEMPLATE: InterpreterBaseTemplate.interpreterBase(...)
// Will be overwritten with every generation.

import { InterpreterContext, RtObject, RtError } from "@freon4dsl/core";
import {
    Node,
    MoveCommand,
    List,
    SetPos,
    SetHeading,
    HomeCommand,
    Repeat,
    If,
    Procedure,
    Parameter,
    Direction,
    Program,
    Library,
} from "../../language/gen/index.js";

/**
 * The base class containing all interpreter functions that should be defined.
 * All functions throw an error when called.
 */
export class LogoInterpreterBase {
    constructor() {}

    evalNode(node: Node, ctx: InterpreterContext): RtObject {
        throw new RtError("evalNode is not defined");
    }

    evalMoveCommand(node: MoveCommand, ctx: InterpreterContext): RtObject {
        throw new RtError("evalMoveCommand is not defined");
    }

    evalList(node: List, ctx: InterpreterContext): RtObject {
        throw new RtError("evalList is not defined");
    }

    evalSetPos(node: SetPos, ctx: InterpreterContext): RtObject {
        throw new RtError("evalSetPos is not defined");
    }

    evalSetHeading(node: SetHeading, ctx: InterpreterContext): RtObject {
        throw new RtError("evalSetHeading is not defined");
    }

    evalHomeCommand(node: HomeCommand, ctx: InterpreterContext): RtObject {
        throw new RtError("evalHomeCommand is not defined");
    }

    evalRepeat(node: Repeat, ctx: InterpreterContext): RtObject {
        throw new RtError("evalRepeat is not defined");
    }

    evalIf(node: If, ctx: InterpreterContext): RtObject {
        throw new RtError("evalIf is not defined");
    }

    evalProcedure(node: Procedure, ctx: InterpreterContext): RtObject {
        throw new RtError("evalProcedure is not defined");
    }

    evalParameter(node: Parameter, ctx: InterpreterContext): RtObject {
        throw new RtError("evalParameter is not defined");
    }

    evalDirection(node: Direction, ctx: InterpreterContext): RtObject {
        throw new RtError("evalDirection is not defined");
    }

    evalProgram(node: Program, ctx: InterpreterContext): RtObject {
        throw new RtError("evalProgram is not defined");
    }

    evalLibrary(node: Library, ctx: InterpreterContext): RtObject {
        throw new RtError("evalLibrary is not defined");
    }
}

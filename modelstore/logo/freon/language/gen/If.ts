// Generated by the Freon Language Generator.
// TEMPLATE ConceptTemplate.generateConceptPrivate(...)

import { MobxModelElementImpl, observablepart, type FreNode, FreParseLocation, notNullOrUndefined, FreUtils } from "@freon4dsl/core";
import { type IExpression, List } from "./internal.js";

// TEMPLATE: ConceptTemplate.generateConceptPrivate
import { makeObservable, action } from "mobx";

/**
 * Class If is the implementation of the concept with the same name in the language definition file.
 * It uses mobx decorators to enable parts of the language environment, e.g. the editor, to react
 * to the changes in the state of its properties.
 */
export class If extends MobxModelElementImpl implements FreNode {
    /**
     * A convenience method that creates an instance of this class
     * based on the properties defined in 'data'.
     * @param data partial object
     */
    static create(data: Partial<If>): If {
        const result = new If(data.$id);
        if (notNullOrUndefined(data.condition)) {
            result.condition = data.condition;
        }
        if (notNullOrUndefined(data.ifTrue)) {
            result.ifTrue = data.ifTrue;
        } else {
            result.ifTrue = List.create({});
        }
        if (notNullOrUndefined(data.ifFalse)) {
            result.ifFalse = data.ifFalse;
        }
        if (notNullOrUndefined(data.parseLocation)) {
            result.parseLocation = data.parseLocation;
        }
        return result;
    }

    readonly $typename: string = "If"; // holds the metatype in the form of a string
    $id: string = ""; // a unique identifier
    parseLocation: FreParseLocation | undefined; // if relevant, the location of this element within the source from which it is parsed

    condition!: IExpression; // implementation of part 'condition'
    ifTrue!: List; // implementation of part 'ifTrue'
    ifFalse: List | undefined; // implementation of part 'ifFalse'

    constructor(id?: string) {
        super();
        if (!!id) {
            this.$id = id;
        } else {
            this.$id = FreUtils.ID(); // uuid.v4();
        }

        // Both 'observablepart' and 'observablepartlist' change the get and set of the attribute
        // such that the parent-part relationship is consistently maintained,
        // and make sure the part is observable. In lists no 'null' or 'undefined' values are allowed.
        observablepart(this, "condition");

        observablepart(this, "ifTrue");

        observablepart(this, "ifFalse");

        // Make copy method a mobx action
        makeObservable(this, {
            copy: action,
        });
    }

    /**
     * Returns the metatype of this instance in the form of a string.
     */
    freLanguageConcept(): string {
        return this.$typename;
    }

    /**
     * Returns the unique identifier of this instance.
     */
    freId(): string {
        return this.$id;
    }

    /**
     * Returns true if this instance is a model concept.
     */
    freIsModel(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a model unit.
     */
    freIsUnit(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is an expression concept.
     */
    freIsExpression(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a binary expression concept.
     */
    freIsBinaryExpression(): boolean {
        return false;
    }
    /**
     * A convenience method that copies this instance into a new object.
     */
    copy(): If {
        const result = new If();
        if (notNullOrUndefined(this.condition)) {
            result.condition = this.condition.copy();
        }
        if (notNullOrUndefined(this.ifTrue)) {
            result.ifTrue = this.ifTrue.copy();
        }
        if (notNullOrUndefined(this.ifFalse)) {
            result.ifFalse = this.ifFalse.copy();
        }
        return result;
    }
    /**
     * Matches a partial instance of this class to this object
     * based on the properties defined in the partial.
     * @param toBeMatched
     */
    public match(toBeMatched: Partial<If>): boolean {
        let result: boolean = true;
        if (result && notNullOrUndefined(toBeMatched.condition)) {
            result = result && this.condition.match(toBeMatched.condition);
        }
        if (result && notNullOrUndefined(toBeMatched.ifTrue)) {
            result = result && this.ifTrue.match(toBeMatched.ifTrue);
        }
        if (result && notNullOrUndefined(toBeMatched.ifFalse)) {
            if (this.ifFalse) {
                result = result && this.ifFalse.match(toBeMatched.ifFalse);
            }
        }
        return result;
    }
}

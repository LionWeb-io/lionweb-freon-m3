// Generated by the Freon Language Generator.
// TEMPLATE: NamespaceCheckerTemplate.generateChecker(...)

import {
    FreError,
    FreErrorSeverity,
    type FreWriter,
    FreLanguageEnvironment,
    FreLanguage,
    FreNamespace,
    type FreNode,
    type FreNamedNode,
    notNullOrUndefined,
} from "@freon4dsl/core";
import {
    Logo,
    Program,
    Library,
    Node,
    MoveCommand,
    List,
    SetPos,
    SetHeading,
    HomeCommand,
    Repeat,
    If,
    Procedure,
    Parameter,
    Direction,
} from "../../language/gen/index.js";

import { LogoDefaultWorker } from "../../utils/gen/index.js";
import { type LogoCheckerInterface } from "./LogoValidator.js";

/**
 * Class LogoNamespaceChecker is part of the implementation of the default validator.
 * It checks whether namespaces, as such defined in the .scope definition, have more than one node
 * with the same name.
 * Class LogoWalker implements the traversal of the model tree. This class implements
 * the actual checking of each node in the tree.
 */
export class LogoNamespaceChecker extends LogoDefaultWorker implements LogoCheckerInterface {
    // 'myWriter' is used to provide error messages on the nodes in the model tree
    myWriter: FreWriter = FreLanguageEnvironment.getInstance().writer;
    // 'errorList' holds the errors found while traversing the model tree
    errorList: FreError[] = [];

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorlist'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeLogo(node: Logo): boolean {
        if (notNullOrUndefined(node) && FreLanguage.getInstance().classifier("Logo")?.isNamespace) {
            this.checkDuplicateNamesInNamespace(node);
        }
        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorlist'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeProgram(node: Program): boolean {
        if (notNullOrUndefined(node) && FreLanguage.getInstance().classifier("Program")?.isNamespace) {
            this.checkDuplicateNamesInNamespace(node);
        }
        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorlist'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeLibrary(node: Library): boolean {
        if (notNullOrUndefined(node) && FreLanguage.getInstance().classifier("Library")?.isNamespace) {
            this.checkDuplicateNamesInNamespace(node);
        }
        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorlist'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeNode(node: Node): boolean {
        if (notNullOrUndefined(node) && FreLanguage.getInstance().classifier("Node")?.isNamespace) {
            this.checkDuplicateNamesInNamespace(node);
        }
        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorlist'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeMoveCommand(node: MoveCommand): boolean {
        if (notNullOrUndefined(node) && FreLanguage.getInstance().classifier("MoveCommand")?.isNamespace) {
            this.checkDuplicateNamesInNamespace(node);
        }
        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorlist'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeList(node: List): boolean {
        if (notNullOrUndefined(node) && FreLanguage.getInstance().classifier("List")?.isNamespace) {
            this.checkDuplicateNamesInNamespace(node);
        }
        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorlist'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeSetPos(node: SetPos): boolean {
        if (notNullOrUndefined(node) && FreLanguage.getInstance().classifier("SetPos")?.isNamespace) {
            this.checkDuplicateNamesInNamespace(node);
        }
        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorlist'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeSetHeading(node: SetHeading): boolean {
        if (notNullOrUndefined(node) && FreLanguage.getInstance().classifier("SetHeading")?.isNamespace) {
            this.checkDuplicateNamesInNamespace(node);
        }
        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorlist'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeHomeCommand(node: HomeCommand): boolean {
        if (notNullOrUndefined(node) && FreLanguage.getInstance().classifier("HomeCommand")?.isNamespace) {
            this.checkDuplicateNamesInNamespace(node);
        }
        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorlist'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeRepeat(node: Repeat): boolean {
        if (notNullOrUndefined(node) && FreLanguage.getInstance().classifier("Repeat")?.isNamespace) {
            this.checkDuplicateNamesInNamespace(node);
        }
        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorlist'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeIf(node: If): boolean {
        if (notNullOrUndefined(node) && FreLanguage.getInstance().classifier("If")?.isNamespace) {
            this.checkDuplicateNamesInNamespace(node);
        }
        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorlist'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeProcedure(node: Procedure): boolean {
        if (notNullOrUndefined(node) && FreLanguage.getInstance().classifier("Procedure")?.isNamespace) {
            this.checkDuplicateNamesInNamespace(node);
        }
        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorlist'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeParameter(node: Parameter): boolean {
        if (notNullOrUndefined(node) && FreLanguage.getInstance().classifier("Parameter")?.isNamespace) {
            this.checkDuplicateNamesInNamespace(node);
        }
        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorlist'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeDirection(node: Direction): boolean {
        if (notNullOrUndefined(node) && FreLanguage.getInstance().classifier("Direction")?.isNamespace) {
            this.checkDuplicateNamesInNamespace(node);
        }
        return false;
    }

    private checkDuplicateNamesInNamespace(node: FreNode) {
        const declaredNodes: Set<FreNamedNode> = FreNamespace.create(node).getDeclaredNodes(false);
        const declaredNames: string[] = [];
        const doubleNames: string[] = [];
        declaredNodes.forEach((nn) => {
            if (declaredNames.indexOf(nn.name) > -1) {
                doubleNames.push(nn.name);
            } else {
                declaredNames.push(nn.name);
            }
        });
        if (doubleNames.length > 0) {
            const namespaceName: string = "name" in node ? (node as FreNamedNode).name : "<unnamed>";
            this.errorList.push(
                new FreError(
                    `Namespace ${namespaceName} has multiple nodes with the same name [${doubleNames.map((n) => n).join(", ")}].`,
                    node,
                    namespaceName,
                    "name",
                    FreErrorSeverity.Error,
                ),
            );
        }
    }
}

// Generated by the Freon Language Generator.
// TEMPLATE: NonOptionalsCheckerTemplate.generateChecker(...)

import { FreError, FreErrorSeverity, type FreWriter, FreLanguageEnvironment, isNullOrUndefined } from "@freon4dsl/core";
import {
    Logo,
    Program,
    Library,
    MoveCommand,
    SetPos,
    SetHeading,
    HomeCommand,
    Repeat,
    If,
    Procedure,
    Parameter,
    Direction,
} from "../../language/gen/index.js";

import { LogoDefaultWorker } from "../../utils/gen/index.js";
import { type LogoCheckerInterface } from "./LogoValidator.js";

/**
 * Class LogoNonOptionalsChecker is part of the implementation of the default validator.
 * It checks whether non-optional properties, as such defined in the .ast definition, indeed
 * have a value.
 * Class LogoWalker implements the traversal of the model tree. This class implements
 * the actual checking of each node in the tree.
 */
export class LogoNonOptionalsChecker extends LogoDefaultWorker implements LogoCheckerInterface {
    // 'myWriter' is used to provide error messages on the nodes in the model tree
    myWriter: FreWriter = FreLanguageEnvironment.getInstance().writer;
    // 'errorList' holds the errors found while traversing the model tree
    errorList: FreError[] = [];

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorlist'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeLogo(node: Logo): boolean {
        if (isNullOrUndefined(node.name) || node.name?.length === 0) {
            this.errorList.push(new FreError("Property 'name' must have a value", node, node.name, "name", FreErrorSeverity.Error));
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorlist'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeProgram(node: Program): boolean {
        if (isNullOrUndefined(node.name) || node.name?.length === 0) {
            this.errorList.push(new FreError("Property 'name' must have a value", node, node.name, "name", FreErrorSeverity.Error));
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorlist'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeLibrary(node: Library): boolean {
        if (isNullOrUndefined(node.name) || node.name?.length === 0) {
            this.errorList.push(new FreError("Property 'name' must have a value", node, node.name, "name", FreErrorSeverity.Error));
        }
        if (isNullOrUndefined(node.procedures)) {
            this.errorList.push(
                new FreError("Property 'procedures' must have a value", node, node.name, "procedures", FreErrorSeverity.Error),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorlist'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeMoveCommand(node: MoveCommand): boolean {
        if (isNullOrUndefined(node.distance)) {
            this.errorList.push(new FreError("Property 'distance' must have a value", node, node.name, "distance", FreErrorSeverity.Error));
        }
        if (isNullOrUndefined(node.name) || node.name?.length === 0) {
            this.errorList.push(new FreError("Property 'name' must have a value", node, node.name, "name", FreErrorSeverity.Error));
        }
        if (isNullOrUndefined(node.direction)) {
            this.errorList.push(
                new FreError("Property 'direction' must have a value", node, node.name, "direction", FreErrorSeverity.Error),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorlist'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeSetPos(node: SetPos): boolean {
        if (isNullOrUndefined(node.x)) {
            this.errorList.push(new FreError("Property 'x' must have a value", node, node.name, "x", FreErrorSeverity.Error));
        }
        if (isNullOrUndefined(node.y)) {
            this.errorList.push(new FreError("Property 'y' must have a value", node, node.name, "y", FreErrorSeverity.Error));
        }
        if (isNullOrUndefined(node.name) || node.name?.length === 0) {
            this.errorList.push(new FreError("Property 'name' must have a value", node, node.name, "name", FreErrorSeverity.Error));
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorlist'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeSetHeading(node: SetHeading): boolean {
        if (isNullOrUndefined(node.degrees)) {
            this.errorList.push(new FreError("Property 'degrees' must have a value", node, node.name, "degrees", FreErrorSeverity.Error));
        }
        if (isNullOrUndefined(node.name) || node.name?.length === 0) {
            this.errorList.push(new FreError("Property 'name' must have a value", node, node.name, "name", FreErrorSeverity.Error));
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorlist'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeHomeCommand(node: HomeCommand): boolean {
        if (isNullOrUndefined(node.name) || node.name?.length === 0) {
            this.errorList.push(new FreError("Property 'name' must have a value", node, node.name, "name", FreErrorSeverity.Error));
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorlist'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeRepeat(node: Repeat): boolean {
        if (isNullOrUndefined(node.count)) {
            this.errorList.push(new FreError("Property 'count' must have a value", node, "unnamed", "count", FreErrorSeverity.Error));
        }
        if (isNullOrUndefined(node.list)) {
            this.errorList.push(new FreError("Property 'list' must have a value", node, "unnamed", "list", FreErrorSeverity.Error));
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorlist'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeIf(node: If): boolean {
        if (isNullOrUndefined(node.condition)) {
            this.errorList.push(
                new FreError("Property 'condition' must have a value", node, "unnamed", "condition", FreErrorSeverity.Error),
            );
        }
        if (isNullOrUndefined(node.ifTrue)) {
            this.errorList.push(new FreError("Property 'ifTrue' must have a value", node, "unnamed", "ifTrue", FreErrorSeverity.Error));
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorlist'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeProcedure(node: Procedure): boolean {
        if (isNullOrUndefined(node.name) || node.name?.length === 0) {
            this.errorList.push(new FreError("Property 'name' must have a value", node, node.name, "name", FreErrorSeverity.Error));
        }
        if (isNullOrUndefined(node.body)) {
            this.errorList.push(new FreError("Property 'body' must have a value", node, node.name, "body", FreErrorSeverity.Error));
        }
        if (isNullOrUndefined(node.paremeter)) {
            this.errorList.push(
                new FreError("Property 'paremeter' must have a value", node, node.name, "paremeter", FreErrorSeverity.Error),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorlist'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeParameter(node: Parameter): boolean {
        if (isNullOrUndefined(node.name) || node.name?.length === 0) {
            this.errorList.push(new FreError("Property 'name' must have a value", node, node.name, "name", FreErrorSeverity.Error));
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorlist'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeDirection(node: Direction): boolean {
        if (isNullOrUndefined(node.name) || node.name?.length === 0) {
            this.errorList.push(new FreError("Property 'name' must have a value", node, node.name, "name", FreErrorSeverity.Error));
        }

        return false;
    }
}

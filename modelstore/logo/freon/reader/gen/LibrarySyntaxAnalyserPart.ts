// Generated by the Freon Language Generator.
import { type KtList, type Sentence, type SpptDataNodeInfo } from "net.akehurst.language-agl-processor";
import { FreNodeReference, notNullOrUndefined } from "@freon4dsl/core";
import { Library, Procedure, List, Parameter, ICommand } from "../../language/gen/index.js";
import { PrimValueType, LogoSyntaxAnalyser } from "./LogoSyntaxAnalyser.js";

export class LibrarySyntaxAnalyserPart {
    mainAnalyser: LogoSyntaxAnalyser;

    constructor(mainAnalyser: LogoSyntaxAnalyser) {
        this.mainAnalyser = mainAnalyser;
    }

    /**
     * Method to transform branches that match the following rule:
     * Library = 'Library' identifier '\{'
     *	 'procedures' Procedure
     *	 '}' ;
     * @param nodeInfo
     * @param children
     * @param sentence
     */
    public transformLibrary(nodeInfo: SpptDataNodeInfo, children: KtList<any>, sentence: Sentence): Library {
        // console.log('4 transformLibrary called: ' + children.toString());
        let __name: string;
        let __procedures: Procedure;
        __name = this.mainAnalyser.transformPrimValue<string>(
            children.asJsReadonlyArrayView()[1] as unknown as string,
            PrimValueType.identifier,
        ); // RHSPrimEntry
        __procedures = children.asJsReadonlyArrayView()[4] as Procedure; // RHSPartEntry

        return Library.create({
            name: __name!,
            procedures: __procedures!,
            parseLocation: this.mainAnalyser.location(sentence, nodeInfo.node),
        });
    }

    /**
     * Method to transform branches that match the following rule:
     * Procedure = 'Procedure' identifier '\{'
     *	 'body' List
     *	 'paremeter' Parameter
     *	 '}' ;
     * @param nodeInfo
     * @param children
     * @param sentence
     */
    public transformProcedure(nodeInfo: SpptDataNodeInfo, children: KtList<any>, sentence: Sentence): Procedure {
        // console.log('4 transformProcedure called: ' + children.toString());
        let __name: string;
        let __body: List;
        let __paremeter: Parameter;
        __name = this.mainAnalyser.transformPrimValue<string>(
            children.asJsReadonlyArrayView()[1] as unknown as string,
            PrimValueType.identifier,
        ); // RHSPrimEntry
        __body = children.asJsReadonlyArrayView()[4] as List; // RHSPartEntry
        __paremeter = children.asJsReadonlyArrayView()[6] as Parameter; // RHSPartEntry

        return Procedure.create({
            name: __name!,
            body: __body!,
            paremeter: __paremeter!,
            parseLocation: this.mainAnalyser.location(sentence, nodeInfo.node),
        });
    }

    /**
     * Method to transform branches that match the following rule:
     * List = 'List' '\{'
     *	 'commands'
     *	 ICommand*
     *	 '}' ;
     * @param nodeInfo
     * @param children
     * @param sentence
     */
    public transformList(nodeInfo: SpptDataNodeInfo, children: KtList<any>, sentence: Sentence): List {
        // console.log('4 transformList called: ' + children.toString());
        let __commands: ICommand[];
        __commands = (children.asJsReadonlyArrayView()[3] as KtList<any>).asJsReadonlyArrayView() as ICommand[]; // RHSPartListEntry

        return List.create({
            commands: __commands!,
            parseLocation: this.mainAnalyser.location(sentence, nodeInfo.node),
        });
    }

    /**
     * Method to transform branches that match the following rule:
     * Parameter = 'Parameter' identifier '\{'
     *	 '}' ;
     * @param nodeInfo
     * @param children
     * @param sentence
     */
    public transformParameter(nodeInfo: SpptDataNodeInfo, children: KtList<any>, sentence: Sentence): Parameter {
        // console.log('4 transformParameter called: ' + children.toString());
        let __name: string;
        __name = this.mainAnalyser.transformPrimValue<string>(
            children.asJsReadonlyArrayView()[1] as unknown as string,
            PrimValueType.identifier,
        ); // RHSPrimEntry

        return Parameter.create({
            name: __name!,
            parseLocation: this.mainAnalyser.location(sentence, nodeInfo.node),
        });
    }
}

// Generated by the Freon Language Generator.
import { type KtList, type Sentence, type SpptDataNodeInfo } from "net.akehurst.language-agl-processor";
import { FreNodeReference, notNullOrUndefined } from "@freon4dsl/core";
import { MoveCommand, SetPos, SetHeading, HomeCommand, Direction, ICommand } from "../../language/gen/index.js";
import { PrimValueType, LogoSyntaxAnalyser } from "./LogoSyntaxAnalyser.js";

export class LogoCommonSyntaxAnalyserPart {
    mainAnalyser: LogoSyntaxAnalyser;

    constructor(mainAnalyser: LogoSyntaxAnalyser) {
        this.mainAnalyser = mainAnalyser;
    }

    /**
     * Method to transform branches that match the following rule:
     * MoveCommand = 'MoveCommand' identifier '\{'
     *	 'distance' numberLiteral
     *	 'direction' Direction
     *	 '}' ;
     * @param nodeInfo
     * @param children
     * @param sentence
     */
    public transformMoveCommand(nodeInfo: SpptDataNodeInfo, children: KtList<any>, sentence: Sentence): MoveCommand {
        // console.log('4 transformMoveCommand called: ' + children.toString());
        let __name: string;
        let __distance: number;
        let __direction: FreNodeReference<Direction>;
        __name = this.mainAnalyser.transformPrimValue<string>(
            children.asJsReadonlyArrayView()[1] as unknown as string,
            PrimValueType.identifier,
        ); // RHSPrimEntry
        __distance = this.mainAnalyser.transformPrimValue<number>(
            children.asJsReadonlyArrayView()[4] as unknown as string,
            PrimValueType.number,
        ); // RHSPrimEntry
        __direction = children.asJsReadonlyArrayView()[6] as FreNodeReference<Direction>; // RHSLimitedRefEntry

        return MoveCommand.create({
            name: __name!,
            distance: __distance!,
            direction: __direction!,
            parseLocation: this.mainAnalyser.location(sentence, nodeInfo.node),
        });
    }

    /**
     * Method to transform branches that match the following rule:
     * SetPos = 'SetPos' identifier '\{'
     *	 'x' numberLiteral
     *	 'y' numberLiteral
     *	 '}' ;
     * @param nodeInfo
     * @param children
     * @param sentence
     */
    public transformSetPos(nodeInfo: SpptDataNodeInfo, children: KtList<any>, sentence: Sentence): SetPos {
        // console.log('4 transformSetPos called: ' + children.toString());
        let __name: string;
        let __x: number;
        let __y: number;
        __name = this.mainAnalyser.transformPrimValue<string>(
            children.asJsReadonlyArrayView()[1] as unknown as string,
            PrimValueType.identifier,
        ); // RHSPrimEntry
        __x = this.mainAnalyser.transformPrimValue<number>(children.asJsReadonlyArrayView()[4] as unknown as string, PrimValueType.number); // RHSPrimEntry
        __y = this.mainAnalyser.transformPrimValue<number>(children.asJsReadonlyArrayView()[6] as unknown as string, PrimValueType.number); // RHSPrimEntry

        return SetPos.create({
            name: __name!,
            x: __x!,
            y: __y!,
            parseLocation: this.mainAnalyser.location(sentence, nodeInfo.node),
        });
    }

    /**
     * Method to transform branches that match the following rule:
     * SetHeading = 'SetHeading' identifier '\{'
     *	 'degrees' numberLiteral
     *	 '}' ;
     * @param nodeInfo
     * @param children
     * @param sentence
     */
    public transformSetHeading(nodeInfo: SpptDataNodeInfo, children: KtList<any>, sentence: Sentence): SetHeading {
        // console.log('4 transformSetHeading called: ' + children.toString());
        let __name: string;
        let __degrees: number;
        __name = this.mainAnalyser.transformPrimValue<string>(
            children.asJsReadonlyArrayView()[1] as unknown as string,
            PrimValueType.identifier,
        ); // RHSPrimEntry
        __degrees = this.mainAnalyser.transformPrimValue<number>(
            children.asJsReadonlyArrayView()[4] as unknown as string,
            PrimValueType.number,
        ); // RHSPrimEntry

        return SetHeading.create({
            name: __name!,
            degrees: __degrees!,
            parseLocation: this.mainAnalyser.location(sentence, nodeInfo.node),
        });
    }

    /**
     * Method to transform branches that match the following rule:
     * HomeCommand = 'HomeCommand' identifier '\{'
     *	 '}' ;
     * @param nodeInfo
     * @param children
     * @param sentence
     */
    public transformHomeCommand(nodeInfo: SpptDataNodeInfo, children: KtList<any>, sentence: Sentence): HomeCommand {
        // console.log('4 transformHomeCommand called: ' + children.toString());
        let __name: string;
        __name = this.mainAnalyser.transformPrimValue<string>(
            children.asJsReadonlyArrayView()[1] as unknown as string,
            PrimValueType.identifier,
        ); // RHSPrimEntry

        return HomeCommand.create({
            name: __name!,
            parseLocation: this.mainAnalyser.location(sentence, nodeInfo.node),
        });
    }

    /**
     * Method to transform branches that match the following rule:
     * ICommand = MoveCommand
     *    | SetPos
     *    | SetHeading
     *    | HomeCommand  ;
     * @param nodeInfo
     * @param children
     * @param sentence
     */
    public transformICommand(nodeInfo: SpptDataNodeInfo, children: KtList<object>, sentence: Sentence): ICommand {
        // console.log('3 transformICommand called: ' + children.toString());
        return children.asJsReadonlyArrayView()[0] as ICommand;
    }

    /**
     * Method to transform branches that match the following rule:
     * Direction = '\`FORWARD\`'
     *	| '\`BACK\`'
     *	| '\`LEFT\`'
     *	| '\`RIGHT\`' ;
     * @param nodeInfo
     * @param children
     * @param sentence
     */
    public transformDirection(
        nodeInfo: SpptDataNodeInfo,
        children: KtList<any>,
        sentence: Sentence,
    ): FreNodeReference<Direction> | undefined {
        // console.log('5 transformDirection called: ' + children.toString());
        let result: FreNodeReference<Direction> | undefined;
        const child: string = children.asJsReadonlyArrayView()[0];
        // todo make sure we remove only the outer quotes
        const name: string = child.replace(/`/g, "");
        switch (name) {
            case "FORWARD": {
                result = FreNodeReference.create<Direction>(Direction.FORWARD, "Direction");
                break;
            }
            case "BACK": {
                result = FreNodeReference.create<Direction>(Direction.BACK, "Direction");
                break;
            }
            case "LEFT": {
                result = FreNodeReference.create<Direction>(Direction.LEFT, "Direction");
                break;
            }
            case "RIGHT": {
                result = FreNodeReference.create<Direction>(Direction.RIGHT, "Direction");
                break;
            }
            default:
                result = undefined;
        }
        if (result !== undefined) {
            result.parseLocation = this.mainAnalyser.location(sentence, nodeInfo.node);
        }
        return result;
    }
}

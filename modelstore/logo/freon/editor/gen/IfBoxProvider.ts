// Generated by the Freon Language Generator.
// TEMPLATE BoxProviderTemplate.generateBoxProvider(...)

import { Box, BoxUtil, FreBoxProvider, FreProjectionHandler, BoxFactory } from "@freon4dsl/core";
import { If } from "../../language/gen/index.js";

/**
 * This class implements the box provider for a single node of type If.
 * The box provider is able to create the (tree of) boxes for the node, based
 * on the projections that are currently selected by the user.
 * The top of the tree of boxes is always a box of type ElementBox, which is
 * a box that will never be rendered itself, only its content will. Thus, we
 * have a stable entry in the complete box tree for every FreNode node.
 */
export class IfBoxProvider extends FreBoxProvider {
    constructor(mainHandler: FreProjectionHandler) {
        super(mainHandler);
        this.knownBoxProjections = ["default"];
        this.knownTableProjections = ["default"];
        this.conceptName = "If";
    }

    protected getContent(projectionName: string): Box {
        // console.log("GET CONTENT " + this._node?.freId() + ' ' +  this._node?.freLanguageConcept() + ' ' + projectionName);
        // see if we need to use a custom projection
        if (!this.knownBoxProjections.includes(projectionName) && !this.knownTableProjections.includes(projectionName)) {
            const BOX: Box = this.mainHandler.executeCustomProjection(this._node, projectionName);
            if (!!BOX) {
                // found one, so return it
                return BOX;
            }
        } else {
            // select the box to return based on the projectionName
            if (projectionName === "default") {
                return this.getDefault();
            }
        }
        // in all other cases, return the default
        return this.getDefault();
    }

    private getDefault(): Box {
        return BoxFactory.verticalLayout(
            this._node as If,
            "If-overall",
            "",
            [
                BoxFactory.horizontalLayout(
                    this._node as If,
                    "If-hlist-line-0",
                    "",
                    [
                        BoxUtil.labelBox(this._node as If, "If", "top-1-line-0-item-0"),
                        BoxUtil.labelBox(this._node as If, "\{", "top-1-line-0-item-1"),
                    ],
                    { selectable: false },
                ),
                BoxUtil.indentBox(
                    this._node as If,
                    4,
                    "1",
                    BoxFactory.horizontalLayout(
                        this._node as If,
                        "If-hlist-line-1",
                        "",
                        [
                            BoxUtil.labelBox(this._node as If, "condition", "top-1-line-1-item-0"),
                            BoxUtil.getBoxOrAction(this._node as If, "condition", "IExpression", this.mainHandler),
                        ],
                        { selectable: false },
                    ),
                ),
                BoxUtil.indentBox(
                    this._node as If,
                    4,
                    "2",
                    BoxFactory.horizontalLayout(
                        this._node as If,
                        "If-hlist-line-2",
                        "",
                        [
                            BoxUtil.labelBox(this._node as If, "ifTrue", "top-1-line-2-item-0"),
                            BoxUtil.getBoxOrAction(this._node as If, "ifTrue", "List", this.mainHandler),
                        ],
                        { selectable: false },
                    ),
                ),
                BoxFactory.optional2(
                    this._node as If,
                    "optional-ifFalse",
                    () => !!(this._node as If).ifFalse,
                    BoxUtil.indentBox(
                        this._node as If,
                        4,
                        "0",
                        BoxFactory.horizontalLayout(
                            this._node as If,
                            "If-optional-ifFalse-hlist-line-0",
                            "",
                            [
                                BoxUtil.labelBox(this._node as If, "ifFalse", "top-2-line-0-item-0"),
                                BoxUtil.getBoxOrAction(this._node as If, "ifFalse", "List", this.mainHandler),
                            ],
                            { selectable: false },
                        ),
                    ),
                    false,
                    BoxFactory.action(this._node, "optional-ifFalse", "ifFalse"),
                ),
                BoxUtil.labelBox(this._node as If, "}", "top-1-line-4-item-0"),
            ],
            { cssClass: "If" },
        );
    }
}

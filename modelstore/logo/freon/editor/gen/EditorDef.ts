// Generated by the Freon Language Generator.
// TEMPLATE: EditorDefTemplate.generateEditorDef(...)

import { FreLanguage, FreProjectionHandler, FreBoxProvider, type FreLanguageConcept } from "@freon4dsl/core";
import { MoveCommand, List, SetPos, SetHeading, HomeCommand, Repeat, If, Procedure, Parameter } from "../../language/gen/index.js";

import {
    MoveCommandBoxProvider,
    ListBoxProvider,
    SetPosBoxProvider,
    SetHeadingBoxProvider,
    HomeCommandBoxProvider,
    RepeatBoxProvider,
    IfBoxProvider,
    ProcedureBoxProvider,
    ParameterBoxProvider,
    ProgramBoxProvider,
    LibraryBoxProvider,
} from "../../editor/gen/index.js";

import { freonConfiguration } from "../../config/FreonConfiguration.js";

const map = new Map([
    // the main map
    [
        // Concept has special projection for (one of) its parts
        "MoveCommand",
        new Map([
            // Projection has special projection for (one of) the parts
            [
                // Projection has special projection for some part
                "default",
                new Map([
                    ["name", ""], // special projection
                    ["distance", ""], // special projection
                    ["direction", ""], // special projection
                ]),
            ],
        ]),
    ],
    [
        // Concept has special projection for (one of) its parts
        "List",
        new Map([
            // Projection has special projection for (one of) the parts
            [
                // Projection has special projection for some part
                "default",
                new Map([
                    ["commands", ""], // special projection
                ]),
            ],
        ]),
    ],
    [
        // Concept has special projection for (one of) its parts
        "SetPos",
        new Map([
            // Projection has special projection for (one of) the parts
            [
                // Projection has special projection for some part
                "default",
                new Map([
                    ["name", ""], // special projection
                    ["x", ""], // special projection
                    ["y", ""], // special projection
                ]),
            ],
        ]),
    ],
    [
        // Concept has special projection for (one of) its parts
        "SetHeading",
        new Map([
            // Projection has special projection for (one of) the parts
            [
                // Projection has special projection for some part
                "default",
                new Map([
                    ["name", ""], // special projection
                    ["degrees", ""], // special projection
                ]),
            ],
        ]),
    ],
    [
        // Concept has special projection for (one of) its parts
        "HomeCommand",
        new Map([
            // Projection has special projection for (one of) the parts
            [
                // Projection has special projection for some part
                "default",
                new Map([
                    ["name", ""], // special projection
                ]),
            ],
        ]),
    ],
    [
        // Concept has special projection for (one of) its parts
        "Repeat",
        new Map([
            // Projection has special projection for (one of) the parts
            [
                // Projection has special projection for some part
                "default",
                new Map([
                    ["count", ""], // special projection
                    ["list", ""], // special projection
                ]),
            ],
        ]),
    ],
    [
        // Concept has special projection for (one of) its parts
        "If",
        new Map([
            // Projection has special projection for (one of) the parts
            [
                // Projection has special projection for some part
                "default",
                new Map([
                    ["condition", ""], // special projection
                    ["ifTrue", ""], // special projection
                    ["ifFalse", ""], // special projection
                ]),
            ],
        ]),
    ],
    [
        // Concept has special projection for (one of) its parts
        "Procedure",
        new Map([
            // Projection has special projection for (one of) the parts
            [
                // Projection has special projection for some part
                "default",
                new Map([
                    ["name", ""], // special projection
                    ["body", ""], // special projection
                    ["paremeter", ""], // special projection
                ]),
            ],
        ]),
    ],
    [
        // Concept has special projection for (one of) its parts
        "Parameter",
        new Map([
            // Projection has special projection for (one of) the parts
            [
                // Projection has special projection for some part
                "default",
                new Map([
                    ["name", ""], // special projection
                ]),
            ],
        ]),
    ],
    [
        // Concept has special projection for (one of) its parts
        "ICommand",
        new Map([
            // Projection has special projection for (one of) the parts
            [
                // Projection has special projection for some part
                "default",
                new Map([
                    ["name", ""], // special projection
                ]),
            ],
        ]),
    ],
    [
        // Concept has special projection for (one of) its parts
        "Program",
        new Map([
            // Projection has special projection for (one of) the parts
            [
                // Projection has special projection for some part
                "default",
                new Map([
                    ["name", ""], // special projection
                    ["commands", ""], // special projection
                ]),
            ],
        ]),
    ],
    [
        // Concept has special projection for (one of) its parts
        "Library",
        new Map([
            // Projection has special projection for (one of) the parts
            [
                // Projection has special projection for some part
                "default",
                new Map([
                    ["name", ""], // special projection
                    ["procedures", ""], // special projection
                ]),
            ],
        ]),
    ],
]);

/**
 * Adds all known projection groups to the root projection.
 * @param handler
 */
export function initializeProjections(handler: FreProjectionHandler) {
    for (const p of freonConfiguration.customProjection) {
        handler.addCustomProjection(p);
    }
    handler.initConceptToPropertyProjection(map);
    handler.initProviderConstructors(
        new Map<string, () => FreBoxProvider>([
            [
                "MoveCommand",
                () => {
                    return new MoveCommandBoxProvider(handler);
                },
            ],
            [
                "List",
                () => {
                    return new ListBoxProvider(handler);
                },
            ],
            [
                "SetPos",
                () => {
                    return new SetPosBoxProvider(handler);
                },
            ],
            [
                "SetHeading",
                () => {
                    return new SetHeadingBoxProvider(handler);
                },
            ],
            [
                "HomeCommand",
                () => {
                    return new HomeCommandBoxProvider(handler);
                },
            ],
            [
                "Repeat",
                () => {
                    return new RepeatBoxProvider(handler);
                },
            ],
            [
                "If",
                () => {
                    return new IfBoxProvider(handler);
                },
            ],
            [
                "Procedure",
                () => {
                    return new ProcedureBoxProvider(handler);
                },
            ],
            [
                "Parameter",
                () => {
                    return new ParameterBoxProvider(handler);
                },
            ],
            [
                "Program",
                () => {
                    return new ProgramBoxProvider(handler);
                },
            ],
            [
                "Library",
                () => {
                    return new LibraryBoxProvider(handler);
                },
            ],
        ]),
    );
    handler.initTableHeaders([]);
}

/**
 * Helper function to ensure that the meta information for typeName can be found.
 * @param typeName
 */
export function conceptRequired(typeName: string): FreLanguageConcept {
    const c = FreLanguage.getInstance().concept(typeName);
    if (!c) {
        throw new Error(`Concept '${typeName}' not found in language.`);
    }
    return c;
}

/**
 * Adds trigger and reference shortcut info to the in-memory representation of structure of the language metamodel.
 */
export function initializeEditorDef() {
    conceptRequired("MoveCommand").trigger = "MoveCommand";
    conceptRequired("List").trigger = "List";
    conceptRequired("SetPos").trigger = "SetPos";
    conceptRequired("SetHeading").trigger = "SetHeading";
    conceptRequired("HomeCommand").trigger = "HomeCommand";
    conceptRequired("Repeat").trigger = "Repeat";
    conceptRequired("If").trigger = "If";
    conceptRequired("Procedure").trigger = "Procedure";
    conceptRequired("Parameter").trigger = "Parameter";

    const conceptProjectionToPropertyProjection = new Map();
}
